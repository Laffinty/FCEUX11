# FCEUX11 v2.1.1.7 — B.6（`$4014` 核对）验证记录

| 项目 | 值 |
|------|-----|
| 记录时间 | 2026-09-12 |
| 分支 / commit | `wip2.1rc2`（验证项，无源码改动） |
| 阶段 | M2 / Step B.6（plan §B.6，原 B.5 降级项） |
| 前置 | B.5-2d `9abccdc`（PAL 专项收尾） |

本项**不产生源码改动**，只核对三条结论。全部已用静态 + 运行时证据确认。

---

## 1. "mapper 的 OAM DMA hook" 不存在（一审前提错误）

```
> rg -n "4014|OAM" src/boards
（0 命中）
```

`rg` 在 mapper 实现目录零命中 → 没有任何 board 安装 `$4014` 相关的 OAM DMA 回调。一审把它列成"待移植项"是基于错误前提；C++ 侧的 `B4014`（`ppu.cpp:1131-1152`）本来就只做 `X6502_DMW(0x2004, ...)`，且 `B2004` 不调用 `PPU_hook`，因此**没有需要迁移的 hook**。

## 2. Rust 侧 `$4014` 异步泵完好

- 状态机：`crates/fceux11-ppu/src/state.rs` 的 `oam_dma_pending` / `oam_dma_page` / `oam_dma_counter` 三字段 + `start_oam_dma()` / `begin_oam_dma()` / `tick_oam_dma()`（每 CPU cycle 泵 1 字节，256 字节后结束并复位 `oam_addr`）。
- 桥在 `$4014` 写路径上的行为未变（B.4 另加了一行 `SpriteDMA = value` 的调试镜像，不影响 DMA 本身）。

## 3. DMC 仲裁接线完好

```
src/sound.cpp:660  static INLINE void DMCDMA(void)
src/sound.cpp:685  // X6502_DMR calls above. `ppu_rust_bridge_dmc_dma_arbitration`
src/sound.cpp:696  ppu_rust_bridge_dmc_dma_arbitration(4);
src/ppu_rust_bridge.cpp:618  void ppu_rust_bridge_dmc_dma_arbitration(uint8_t stall_cycles) {
src/ppu_rust_bridge.cpp:619      if (g_ppu_state == nullptr) return;
src/ppu_rust_bridge.cpp:620      fceux11_ppu_dmc_dma_arbitration(g_ppu_state, stall_cycles);
```

调用链 C++ APU → 桥 → Rust 状态机完整；`npm` 仲裁的消费端（`fceux11_ppu_take_dmc_dma_stall`）在交错循环里仍在（B.5-2c 未触碰该分支）。

## 4. 运行时证据：不退化

`ctest --test-dir build-rust-ppu -R "^kagami_qa_direct_smoke$" -V`：

```
[direct] blargg_sprdma_dmc_dma: FAIL
[direct] blargg_vbl_05_nmi_timing: PASS
[direct] blargg_cpu_instrs: PASS
...
Direct run complete: 12 total, 4 PASS, 8 FAIL (0 blocking)
100% tests passed  (baseline 比较通过)
```

- `blargg_sprdma_dmc_dma` 的状态与冻结基线**完全一致**：`tests/fixtures/kagamiqa_baseline_frozen.json` 里 `"blargg_sprdma_dmc_dma": false`（= 已知接受失败），且本次 **0 blocking**、baseline 比较通过 → 属既定状态，不是本 sub-plan 引入的回归。
- `nestest` 由 `rom_regression_rust_smoke`（780 帧逐帧 CRC）覆盖，在 B.5-2d 的全量 gate 中 PASS。
- 全量 gate：**43 项 = 42 PASS / 1 SKIP / 0 FAIL**（详见 B.5-2d 记录）。

## 5. 结论与遗留

- **B.6 结论**：不存在待移植的 mapper OAM DMA hook；Rust `$4014` 泵与 DMC 仲裁完好；相关回归面（sprdma / nestest）不退化。
- **留到 Step C 之后的复核**：plan §B.6 要求确认"Step C 删 C++ PPU 后不受影响"。Step C（M4）尚未执行，因此该复核**并入 M5（Step E）**：届时重跑 `rom_regression_rust_smoke` + `blargg_rust_smoke` + kagami 矩阵即可覆盖。

---

*本记录依据：`rg -n "4014|OAM" src/boards`（0 命中）、`src/rust/crates/fceux11-ppu/src/state.rs` 的 OAM DMA 实现、`src/sound.cpp:660-696` 与 `src/ppu_rust_bridge.cpp:618-621` 的仲裁接线、`ctest -R kagami_qa_direct_smoke -V` 输出、`tests/fixtures/kagamiqa_baseline_frozen.json`、B.5-2d 全量 gate 日志。*