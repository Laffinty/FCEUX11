# FCEUX11 v2.1.1.7 Step C 落地记录（C++ PPU 编译分支删除）

- 记录时间：2026-09-12
- 分支：`wip2.1rc2`
- 阶段：M4（C.0 冻结参照已于 `d3d6438` 落地）
- 前置：M0 至 M3 全部完成，断点 gate 43 项全绿

## 1. 落地内容

### 1.1 删除的编译单元（C.1）

- `src/ppu.cpp`（1221 行）、`src/ppu_rendering.cpp`（2248 行）与 `src/ppu_rendering.h`
- `src/ppu_core.cpp`，以及 `src/ppu_core.h` 中的引擎声明
- `src/ppu_sprite_lut.cpp/h`、`src/pputile_template.cpp/h`、`src/pputile.inc`
- 净删约 4.4k 行；`src/CMakeLists.txt` 的 `SRC_CORE` 同步移除，并删掉 `ppu_sprite_lut.cpp` 的 `/GL-` 特例与 `ppu_rust_bridge.cpp` 的条件登记

### 1.2 代码迁入 `src/ppu_shared.cpp`

- 生命周期与派发：`FCEUPPU_Init/Power/Reset/Loop/SetVideoSystem/PeekAddress`、`PPU_ResetHooks`、`newppu_get_scanline/dot`、`newppu_hacky_emergency_reset`
- mapper 钩子：`PPU_hook` / `GameHBIRQHook` / `GameHBIRQHook2`
- PPU 空间总线派发：`FFCEUX_PPURead` / `FFCEUX_PPUWrite` 及两者的 `_Default` 实现
- C++ 权威数组与访问器：`PALRAM` / `UPALRAM` / `READPAL_MOTHEROFALL` / `FCEUPPU_GetCHR` / `FCEUPPU_GetAttr`
- 必须保留的转发：`FCEUPPU_SetVideoSystem` 仍调用桥的 `set_video_system`（否则 PAL/Dendy 静默退回 NTSC）；`FCEUPPU_PeekAddress` 仍返回桥的 Rust v

### 1.3 新增墓碑 TU：`src/ppu_legacy_stub.cpp`

- 只保留仍被取地址或引用的引擎期对象：`ppur` / `spr_read` / `idleSynch` / `new_ppu_reset` / `SPRBUF` / `linestartts`
- 初值与 Step C 之前一致；文件头写明：不得用于行为读取，Rust PPU 是权威
- savestate 不经过这些字节：chunk-3 / chunk-31 自 B.1 起走 `src/ppu_bridge_state.cpp` 的 staging

### 1.4 测试目标

- 退役 `ppu_rendering_lut_test`（LUT 实现已删除）与 `ppu_phase_d_test`（本地副本算法，删源码后成为自证测试）
- 保留 `ppu_phase_c_test`（`SPRBUF` 布局由墓碑继续提供）
- `tests/CMakeLists.txt` 与 `tests/tests.json` 同步移除两个条目

### 1.5 C.2：configure 期错误

- `option(FCEUX11_RUST_PPU ...)` 保留声明，但 OFF 直接 `FATAL_ERROR`（文案对齐 `FCEUX11_RUST_CPU=OFF`）
- 删除 `src/CMakeLists.txt` 的防御性 `set(FCEUX11_RUST_PPU ON)` 与桥的条件登记；`ppu_rust_bridge.cpp` 无条件进入 `SRC_CORE`
- 源码内 `#ifdef FCEUX11_RUST_PPU` 分支保留：测试目标默认不带该宏，ppu_rust_bridge.h 的 OFF 内联 stub 仍是它们的接口面（plan 7.4）

### 1.6 C.3：脚本与文档

- scripts/run_matrix.ps1 从 build/ 改指 build-rust-ppu/（runner 路径、DLL PATH、矩阵输出）
- AGENTS.md：删除 Gotcha 11，改写构建目录说明与 CMake 选项表
- docs/BuildGuide.md 8.5 节选项表、readme.md 构建目录说明同步；docs/ChangeLog.md 新增 Removed 段

## 2. gate 结果

- 命令：do_build.ps1 -Config Release -BuildDir build-rust-ppu
- 结果：41 项 = 40 PASS / 1 SKIP / 0 FAIL（Step C 之前为 43 项，删掉 2 个测试目标后逐项对应）
- SKIP 仍为 test 33 rust_ppu_vbl_nmi_timing_test（owner 批准的 Route C）
- 关键 gate 全绿：golden_savestate_test 8/8、savestate_regression_rust_smoke 12/12、rom_regression_rust_smoke（780 帧逐帧 CRC）、ppu_frame_diff_test（5 ROM 逐像素）、mapper 全家 byte-diff、newppu_neutrality_test、pal_timing_test、bench_tolerance_test

## 3. 偏差与遗留

- 中断与重跑：本批首次执行在 2026-09-12 15:32 前后被中断（源码已落盘、构建未完成）。恢复后从工作树原样续跑，未回退任何 commit；重建后 gate 全绿。
- 源码内 FCEUX11_RUST_PPU 条件分支与 ppu.h、ppu_state.h 的陈旧注释留到后续批次；不影响行为。
- plan C.2 的「删除 option」按「保留 option + FATAL_ERROR」实现：整删 option 会让 -DFCEUX11_RUST_PPU=OFF 被静默忽略，反而丢失报错。
- owner 待办（不阻塞 M5）：GUI 手动 QA、Dendy 帧结构裁定、可选 B.3a 度量。

## 4. 下一步（M5 / Step E）

1. 全量 gate 重跑 + kagami 抽样，与 M0 基线逐项比对
2. 用 rg 复扫 plan 0.4 清单，确认无遗留直读点
3. 归档本 sub-plan 到 docs/history/，回填 owner 项

---


依据：C.0 提交 d3d6438 + 本批 C.1/C.2/C.3 改动；do_build.ps1 -Config Release -BuildDir build-rust-ppu 全量日志（1045 target + 41 项 ctest）；plan docs/plans/v2.1.1.7_cpp_ppu_removal.md 的 C.1 至 C.3 与 7.4 节。
