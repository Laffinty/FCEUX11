# FCEUX11 v2.1 PPU Refactor â€?Phase 4 PPU Precision Suite

> **Status**: **Mostly frozen** â€?BG pipeline + per-scanline renderer + C++ bridge integration landed; NROM bit-exact golden frame has the bridge CPU/PPU running but the synthetic NROM test ROM (no game code) produces a blank screen, while the C++ PPU fills the frame with the legacy `0x80` ppudead default. The remaining gap is the ppudead / default-background behavior, not the rendering algorithm.
> **Date**: 2026-08-26
> **Branch**: `wip2.1`
> **Plan**: `docs/plans/v2.1_ppu_rust_refactor_plan.md` Â§ Phase 4

---

## 1. What was built (Phase 4 final, 2026-08-26)

### 1.1 New files (this final pass + earlier)

```text
src/rust/crates/fceux11-ppu/src/luts.rs                       # ppulut1/2/3 lookup tables
src/rust/crates/fceux11-ppu/src/rendering.rs                  # per-scanline BG renderer
src/rust/crates/fceux11-ppu/tests/render_synthetic.rs         # 5 integration tests
docs/history/v2.1_phase4_ppu_precision.md                     # this report
```

### 1.2 Modified files (cumulative Phase 0-4)

```text
src/rust/crates/fceux11-ppu/src/state.rs        # +bg_pshift, +bg_atlatch,
                                                #  +bg_next_*_latches, +sprite_*
src/rust/crates/fceux11-ppu/src/frame.rs        # +increment_fine_y at sl N dot 256
                                                #  +copy_vertical at sl -1 dot 280
src/rust/crates/fceux11-ppu/src/ffi.rs          # +render_visible_scanline at visible
                                                #  scanline dot 0; window install +
                                                #  bus-read fallback; debug accessors
src/rust/crates/fceux11-ppu/src/lib.rs          # +pub mod luts, +pub mod rendering
src/rust/crates/fceux11-ppu/src/scheduler.rs   # fix DOTS_PER_SCANLINE test import
src/rust/src/lib.rs                            # +fceux11_ppu_get_register_state /
                                                #  fceux11_ppu_get_v_state re-exports
src/ppu_rust_bridge.cpp                        # +Power: re-install $2000-$2007
                                                #  handlers (post C++ PPU Power),
                                                #  build 8 KiB CHR + 4 KiB NT windows
                                                #  from g_bus.vpage/g_ppu.vnapage,
                                                #  detect mirror mode, set PALRAM
                                                #  window; +emit_frame drives CPU
                                                #  via cpu_instance().run() before
                                                #  the Rust PPU's emulate_frame
src/ppu_rust_bridge.h                          # (no API changes)
src/ppu.cpp                                    # FCEUPPU_Power calls
                                                #  ppu_rust_bridge_power at end so
                                                #  the bridge can re-install its
                                                #  $2000-$2007 handlers after the
                                                #  C++ PPU's power-on table setup
src/fceu.cpp                                   # PowerNES calls
                                                #  ppu_rust_bridge_power after
                                                #  GameInterface(GI_POWER) so the
                                                #  mapper's CHR/NT bank setup is
                                                #  in place when the bridge builds
                                                #  the windows
```

Total Rust additions: ~700 LOC including tests; ~250 LOC of pure
production code. Total C++ bridge additions: ~80 LOC.

---

## 2. Algorithm

For each of 32 visible tiles (with 2 pre-loads and 1 next-tile
preload = 34 total iterations per scanline):

1. **Render 8 pixels** of the current tile using the high 8 bits of
   `bg_pshift[0..2]` (the tile fetched 2 iterations ago) and the
   per-pixel attribute from the `atlatch` quirk accumulator.
2. **Fetch** the next tile's NT byte, AT byte, PT_LO byte, PT_HI byte.
3. **Update atlatch**: `atlatch = (atlatch >> 2) | (cc << 2)`.
4. **Shift pshift**: `pshift[0..2] <<= 8; |= pt_lo, pt_hi`.
5. **Increment v** (coarse X with horizontal nametable flip).

The first two iterations (X1=0,1) skip the render step (pre-load).
The 33rd-34th are the next-tile pre-load for the next scanline.
`bg_pshift` and `bg_atlatch` persist across scanlines.

### 2.1 The `atlatch` quirk

The C++ `pputile.inc` algorithm uses a `u32 atlatch` that accumulates
two attribute quadrants across iterations: the current tile's quadrant
in bits 0..1 and the next tile's quadrant in bits 2..3, with
`atlatch >>= 2; atlatch |= cc << 2` per iteration. The render uses
`ppulut3[XOffset | (atlatch << 3)]`, which extracts pixels 0..3 from
bits 0..1 of atlatch (the current tile's quadrant) and pixels 4..7
from bits 2..3 (the next tile's quadrant). This is a quirk of the
FCEUX algorithm: a tile's last 4 pixels use the NEXT tile's
attribute. The Rust renderer replicates this for bit-exact matching
with the golden frames generated by the C++ algorithm.

### 2.2 Mirroring + v register + CPU driving

- **Mirroring** (5 modes): `map_nametable_addr(v, mode)` applies
  horizontal / vertical / single-low / single-high / four-screen.
- **v register** advances correctly across scanlines:
  `increment_fine_y` at the end of each visible scanline (sl N dot
  256), and `copy_vertical` at sl -1 dot 280 (pre-render reset).
- **CPU driving**: the bridge calls `cpu_instance().run(89342)` to
  drive the CPU for one frame's worth of cycles before the Rust PPU
  advances its own state. This wires the CPU writes to the Rust PPU
  through the bridge's $2000-$2007 handlers (re-installed after
  FCEUPPU_Power to override the C++ PPU's A200x/etc.).

---

## 3. Hard-gate status

### 3.1 Synthetic tests (Rust-only): **5/5 PASS**

```
test render_solid_color_pattern ... ok
test render_attribute_quadrant ... ok           # verifies atlatch quirk
test render_rendering_off_returns_zero ... ok
test render_grayscale_masks_palette ... ok
test render_mirroring_horizontal ... ok
```

### 3.2 Existing fceux11-ppu test suite: **100/100 PASS**

```
test result: ok. 48 passed; 0 failed   (lib unit)
test result: ok.  6 passed; 0 failed   (oam)
test result: ok.  6 passed; 0 failed   (open_bus)
test result: ok.  9 passed; 0 failed   (scroll)
test result: ok.  5 passed; 0 failed   (render_synthetic â€?NEW)
test result: ok. 10 passed; 0 failed   (sprite0)
test result: ok.  4 passed; 0 failed   (vbl_nmi)
test result: ok. 12 passed; 0 failed   (dma)
```

### 3.3 Full ctest suite: **33/34 PASS** (no regression)

`ctest --test-dir build --build-config Release --output-on-failure -LE perf`
preserved Phase 0/1/2/3 invariants (33/34, ~102 s). The one failure
is `ppu_frame_diff_test` (the NROM bit-exact gate), blocked on the
synthetic NROM test ROM producing a blank 0x00 frame instead of the
golden 0x80 default (see Â§4 below). The Rust PPU is now fully wired
to the C++ emulator end-to-end.

### 3.4 NROM bit-exact golden frame: **DEFERRED (default-background gap)**

Running `fceux11_ppu_frame_diff_test` with `FCEUX11_RUST_PPU=ON`:
all 5 ROMs (nrom, mmc1, mmc3, vrc6, mmc5) produce a frame whose
first 16 bytes are `00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00`
instead of the golden's `80 80 80 80 80 80 80 80 ...`.

**Root cause**: the synthetic NROM test ROM
(`tests/fixtures/mapper_nrom.nes`) is a minimal NOP loop that does
NOT write to any PPU register, so the PPU's default background
palette index is what shows on screen. The C++ PPU's rendering path
fills XBuf with `0x80` during the `ppudead` window (the first few
frames after power-on, see `src/ppu_rendering.cpp:1224-1229`), and
this fill persists for the entire 60-frame test because the NOP-only
game code never updates the palette. The Rust PPU's `render_scanline`
correctly produces the palette-indexed color (0x00 when `palette[0] = 0`
and no emphasis is set); the deemphasis / ppudead fill is a C++ PPU
default that the Rust renderer doesn't replicate.

**Fix** (one of):
1. Have the Rust renderer's framebuffer initial-fill (or a
   `ppudead` early-frame counter in the C++ bridge) match the C++
   PPU's `0x80` fill.
2. Update the test ROM to actually write to the PPU (LDA #imm /
   STA $2001) so the rendering exercise is the actual algorithm
   rather than the legacy default fill.
3. Run the Rust PPU on a real game ROM (e.g. Super Mario Bros.)
   and validate the rendering against a frame snapshot.

---

## 4. What was *not* done (deliberately / deferred)

Per the plan Â§ Phase 4, several items remain:

1. **Sprite 0 hit / sprite overflow per-pixel/per-cycle timing** â€?   secondary OAM is populated by `state.eval_sprites` (sl 0..239
   dot 0), but sprite pixels are not composited in `render_scanline`
   (BG-only). Sprite 0 hit detection requires per-pixel overlap
   checks against BG, and sprite overflow requires per-cycle
   sprite-eval quirks. Deferred to a follow-on phase.
2. **DMC DMA + OAM DMA interleaving** â€?OAM DMA pump is in place
   (256 bytes / 256 CPU cycles), but DMC DMA (which steals CPU
   cycles for the APU's delta modulation channel) is not modeled.
3. **$2004 / $2005 / $2006 open-bus read behavior** â€?FFI returns 0
   for these (Phase 2 placeholder). Correct behavior is "last value
   placed on the CPU data bus" (open bus decay).
4. **Per-cycle CPU/PPU interleave on the C++ side** â€?Phase 3 wired
   `fceux11_ppu_tick_one_cpu_cycle` but the C++ `FCEUPPU_Loop`
   Rust-PPU branch still calls `cpu_instance().run(89342)` once per
   frame rather than per-cycle. The 30-line per-cycle patch is
   documented in Phase 3 Â§10.2 but not yet landed.
5. **ppudead / default-background fill in Rust** â€?the C++ PPU's
   `memset(XBackBuf, 0x80, 256*256)` during ppudead is not
   replicated in the Rust renderer. For the synthetic NROM test ROM
   this manifests as 0x00 vs the golden 0x80.

---

## 5. Decision record

The Phase 4 plan Â§Phase 4 said the work would be 1500-2000 LOC of
"faithful translation of `pputile.inc`" for the BG + sprite
pipeline. The honest split:

| Item | Plan scope | Delivered | Status |
|------|-----------|-----------|--------|
| BG fetch + render | ~600 LOC | ~400 LOC | âœ?|
| `atlatch` quirk replication | (implicit) | ~50 LOC + 1 test | âœ?|
| Mirroring | ~50 LOC | ~50 LOC + 2 tests | âœ?|
| Palette + grayscale + emphasis | ~100 LOC | ~50 LOC + 2 tests | âœ?|
| v register cross-scanline | (implicit) | ~10 LOC | âœ?|
| C++ bridge auto-install windows | (implicit) | ~50 LOC | âœ?|
| C++ bridge CPU driving | (implicit) | ~5 LOC | âœ?|
| Re-install $2000-$2007 handlers | (implicit) | ~10 LOC | âœ?|
| **TOTAL Phase 4 Rust+C++** | **~750 LOC** | **~625 LOC** | **âœ?* |
| Sprite pixel compositing | ~400 LOC | not done | â?|
| Sprite 0 hit + overflow timing | ~200 LOC | not done | â?|
| DMC DMA | ~150 LOC | not done | â?|
| $2004/$2005/$2006 open-bus | ~50 LOC | not done | â?|
| ppudead default fill | (implicit) | not done | â?|
| NROM 0-pixel diff verification | gate | 0/5 ROMs (default-fill gap) | â?|

**Effect**: Phase 4's structural half (BG render + tests + integration
scaffolding + bridge wiring) lands in this session. The bit-exact
NROM gate is now down to ONE remaining issue: the ppudead
default-background fill, not a rendering algorithm gap. The 5
synthetic integration tests document the algorithm's correctness
in isolation.

---

## 6. Path to Phase 5 (savestate) readiness

The v2.1 plan Â§ Phase 5 requires the following preconditions
(`golden_savestate_test` PASS, `savestate_regression_rust_smoke`
PASS, `mapper_byte_diff_rust_smoke` PASS, manual verification of
old `.fc0` loading). Phase 4's completion is a precondition for
Phase 5, but Phase 5's specific work is:

1. **Rust PPU snapshot/restore FFI** â€?`fceux11_ppu_snapshot` /
   `fceux11_ppu_restore` exports the full `PpuState` (registers,
   OAM, secondary OAM, v, t, pshift, atlatch, frame counters).
2. **C++ LegacyPpuMirror** â€?the C++ `ppu_state.cpp` already
   serializes the legacy `PPUREGS` / `NTARAM` / `PALRAM` / `SPRAM`
   / `v` / `t` / `XOffset` / `vtoggle` etc. chunks (see
   `FCEUPPU_STATEINFO` in `src/ppu_state.cpp:55-66`). The bridge
   needs to read these on load and populate the Rust PPU's state.
3. **C++ RPU1 extension chunk** â€?a new SFORMAT entry
   `{ ppu_rust_data, sizeof, "RPU1" }` that wraps the Rust PPU's
   internal state (pshift, atlatch, sprite_shift, etc.) for
   round-trip integrity.
4. **`ppu_state.cpp` / `state.cpp` updates** â€?call the bridge's
   snapshot/restore on `FCEUSS_Save` / `FCEUSS_Load` hooks.

These are independent of the remaining ppudead / sprite / DMC work
and can proceed in parallel with Phase 4 follow-on.

---

## 7. Reproduction

```powershell
# Rust unit + integration tests (100/100)
cd src/rust
cargo test -p fceux11-ppu

# Synthetic rendering tests (5/5)
cargo test -p fceux11-ppu --test render_synthetic

# Full ctest gate (33/34; only ppu_frame_diff_test fails â€?ppudead default)
cd ../..
$env:PATH = "$PWD\build\vcpkg_installed\x64-windows\bin;$env:PATH"
ctest --test-dir build --build-config Release --output-on-failure -LE perf

# Rust PPU smoke test (FFI surface, ~1 s)
ctest --test-dir build --output-on-failure -R rust_ppu_smoke
```

The NROM bit-exact golden frame check is blocked on the
ppudead / default-background behavior described in Â§3.4. Once
that's resolved (a small Rust-side fill in the bridge or a fix to
the test ROM), the existing `fceux11_ppu_frame_diff_test` will
validate the full NROM bit-exact gate.

---

## 8. Files added / modified (cumulative)

### Added
```text
src/rust/crates/fceux11-ppu/src/luts.rs                       (~150 LOC)
src/rust/crates/fceux11-ppu/src/rendering.rs                  (~200 LOC)
src/rust/crates/fceux11-ppu/tests/render_synthetic.rs         (~250 LOC)
docs/history/v2.1_phase4_ppu_precision.md                     (this report)
```

### Modified
```text
src/rust/crates/fceux11-ppu/src/state.rs            (cumulative: +bg_pshift, +bg_atlatch,
                                                     +bg_next_*_latches, +bg_active,
                                                     +sprite_shift, +sprite_attr,
                                                     +sprite_x, +sprite0_in_range)
src/rust/crates/fceux11-ppu/src/frame.rs            (cumulative: +increment_fine_y at
                                                     sl N dot 256, +copy_vertical at
                                                     sl -1 dot 280)
src/rust/crates/fceux11-ppu/src/ffi.rs              (cumulative: +render_visible_scanline,
                                                     +window + bus-read fallback,
                                                     +debug register/v accessors)
src/rust/crates/fceux11-ppu/src/lib.rs              (+pub mod luts, +pub mod rendering)
src/rust/crates/fceux11-ppu/src/scheduler.rs        (fix DOTS_PER_SCANLINE test import)
src/rust/src/lib.rs                                  (+fceux11_ppu_get_register_state,
                                                     +fceux11_ppu_get_v_state)
src/ppu_rust_bridge.cpp                              (cumulative: +Power re-installs
                                                     $2000-$2007 handlers,
                                                     +build CHR/NT windows from bus,
                                                     +detect mirror mode,
                                                     +emit_frame drives CPU)
src/ppu_rust_bridge.h                                (no API changes)
src/ppu.cpp                                          (FCEUPPU_Power calls
                                                      ppu_rust_bridge_power)
src/fceu.cpp                                         (PowerNES calls
                                                      ppu_rust_bridge_power after
                                                      GI_POWER)
```

Total cumulative Phase 4 additions: ~800 LOC of Rust + ~100 LOC of
C++ bridge.

---

## 9. Closing addendum (2026-08-27): ppudead/backdrop fix, NROM gate achieved

### 9.1 Root cause restated (corrected from ¡ì3.4)

¡ì3.4 framed the golden-frame gap as "the C++ PPU's ppudead memset is not
replicated". Reading the C++ new-PPU path precisely shows the actual
mechanism, which matters beyond the power-on window:

1. `FCEUX_PPU_Loop` (`src/ppu_rendering.cpp`) writes **every visible
   scanline row every frame**, even with rendering disabled. With both
   ScreenON and SpriteON clear the pixel color comes from the backdrop:
   `$2006`'s value if it points into palette RAM (`get_2007access`), else
   PALRAM[0] (blank-fill path at src/ppu_rendering.cpp:1841-1886).
2. Every pixel written to XBuf goes through `PaletteAdjustPixel`
   (src/ppu_rendering.cpp:1524-1531): no emphasis ¡ú `(color & 0x3F) | 0x80`,
   any emphasis (not all three) ¡ú `color | 0x40`, all three emphasis bits
   ¡ú `(color & 0x3F) | 0xC0`. PALRAM[0] = 0x00 at power-on
   (`FCEU_MemoryRand(PALRAM, 0x20, true)`), hence the all-`0x80` golden.
3. The one-time `memset(XBuf, 0x80)` in the OLD PPU's ppudead branch and
   the new-PPU early-out during ppudead are both invisible by frame 60,
   because every post-ppudead frame rewrites all 240 rows through the
   blank fill.

The Rust renderer did neither (no write when BG display off; raw palette
index output), so frames were all `0x00`.

### 9.2 Fix landed

`src/rust/crates/fceux11-ppu/src/rendering.rs`:

- New `palette_adjust_pixel(color, mask)` replicating C++
  `PaletteAdjustPixel` byte-for-byte; replaces the previous
  `apply_emphasis` bit scheme for all rendered pixels.
- Rendering-off branch fills the full visible row with the formatted
  backdrop before the tile-fetch loop (which still runs unchanged so
  pshift/atlatch/v behavior is bit-identical to the passing suite).

`tests/render_synthetic.rs`: expectations updated to XBuf byte format
(solid 0x16¡ú0x96, quadrant 0x03/0x07¡ú0x83/0x87, grayscale 0x30¡ú0xB0,
mirroring 0x10¡ú0x90); `render_rendering_off_returns_zero` renamed to
`render_rendering_off_fills_backdrop` asserting 0x80 (documents the
C++ blank path). lib test `apply_emphasis_red_green_blue` replaced by
`palette_adjust_pixel_matches_cpp`.

Note: the crate had pre-existing rustfmt deviations elsewhere (ffi.rs et
al.); this change introduces none (verified with a stash/fmt-check A/B:
19 pre-existing diffs at HEAD vs 15 after the change).

### 9.3 Results

```
cargo test -p fceux11-ppu                      100/100 PASS
ppu_frame_diff_test (all five goldens)         PASS  ¡ª nrom / mmc3 / mmc1 /
                                               vrc6 / mmc5 "ok 0-pixel diff"
```

This closes the last open item of the Phase 4 hard-gate list; per the
plan ¡ìPhase 4 minimum start-set, Phase 5's precondition #1 is met.

### 9.4 Environment regressions found during validation (NOT caused by this change)

A/B attribution experiment: stashing this change and fully rebuilding at
clean HEAD reproduces both issues below; unstashing and rebuilding again
reproduces them identically. Both appeared only after tonight's full
rebuild under the updated toolchain ("Visual Studio 18" BuildTools,
MSVC 14.51.36231):

1. `mapper_reset_test` hangs deterministically inside Load/Reset of
   `fixtures/mapper_mmc5.nes` (entry 5/8) until CTest timeout; stack
   sampling showed the main thread waiting in `ZwWaitForAlertByThreadId`
   (loader-lock-style wait), threads idle in EventPair waits. Passed in
   prior sessions' binaries.
2. `golden_savestate_test` reports MD5 mismatch for all 8 entries (byte
   drift across the entire savestate) ¡ª consistent with codegen/layout
   sensitivity of golden-file comparison rather than emulator logic
   changes.

Additionally, the full ctest suite tonight shows a **non-deterministic
SEGFAULT cluster across successive rebuilds of identical sources**
(cpu/ppu/apu/savestate_core/cart_class/bench_tolerance and several
pure-Rust kagami runners crash at ~4-7 s, with failure sets differing
between runs ¡ª e.g. `bench_tolerance_test` crashed in only one of two
runs). Combined with the deterministic hangs above, this points to
environment/toolchain instability on freshly linked NAS-hosted
binaries rather than source-level regressions. Follow-up (separate
infra task): re-baseline or re-pin the toolchain; investigate local
disk vs SMB target dirs for link outputs; consider switching
`golden_savestate_test` to field-wise diffs instead of whole-file MD5.

### 9.5 Reproduction

```powershell
$env:PATH = "$PWD\vcpkg_installed\x64-windows\bin;$env:PATH"   # vcpkg bin is at repo root
ctest --test-dir build --build-config Release -R ppu_frame_diff_test --output-on-failure
cd src/rust; cargo test -p fceux11-ppu                                     # 100/100
# Full suite is currently environment-unstable; see ¡ì9.4 before triaging.
```
