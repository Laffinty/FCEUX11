# FCEUX11 v2.1.1.7 — C.0 冻结参照系（Step C 前置）

| 项目 | 值 |
|------|-----|
| 记录时间 | 2026-09-12 |
| 分支 | `wip2.1rc2`（本地与 `origin/wip2.1rc2` 同步） |
| 冻结 tag | `v2.1.1-cpp-ppu-reference` -> `84041fe` |
| tag 语义 | M3 / Step D batch 2（GUI 写穿 + OAM write FFI）完成 commit，即**删除 C++ PPU 前的最后一个源码 commit** |
| tag 状态 | **仅本地创建，未推送**（plan §C.0：owner 确认后再 push） |
| 用途 | Step C（M4）删除 C++ PPU 之后，ctest / savestate / batch_compat 的"C++ 引擎侧"永久对照；任何退化以本参照系归因 |

> 说明：`wip2.1rc2` 在 `84041fe` 之后还有 docs-only commit（`29c8be2`，§7 断点交接改写），因此"当前 HEAD"与"冻结 tag"相差一个文档 commit，源码内容一致。

---

## 1. 冻结内容与入口

| 对标项 | 冻结产物 | 位置 |
|--------|----------|------|
| ctest 基线（本 sub-plan 口径，`-LE perf`） | 40 项 = 39 PASS / 1 SKIP / 0 FAIL（141.22 s） | `docs/history/v2.1.1.7_m0_baseline.md` §2.1 |
| ctest 全量（含 perf） | 41 项 = 40 PASS / 1 SKIP / 0 FAIL（162.71 s） | 同上 §2.2 |
| KagamiQA 矩阵 / Grade | 47 = 38 PASS / 9 FAIL，**Grade B**，`pass_to_fail = []`；`run_id = 20260910-122744-b50a70`，`git_rev = 4afcd8d` | 同上 §3；冻结基线 `tests/fixtures/kagamiqa_baseline_frozen.json` |
| Oracle A savestate（12 ROM MD5） | `tests/fixtures/golden_savestate_hashes.json`（文件 MD5 `f0d3da0e94a6d6097c14ec6428b2609f`） | 同上 §4.1 |
| SFORMAT 字节比对（8 组） | `tests/fixtures/golden/golden_index.json`（文件 MD5 `b04a27ea49b9f3bb6ef9db6d0594ffd9`）+ 8 个 `.fc0` | 同上 §4.2 |
| 产物尺寸（做差基准） | `fceux11.exe` 7,052,288 B / `fceux11_core.lib` 99,536,558 B / `fceux11_rust.lib` 6,924,740 B | 同上 §5 |

以上数据全部产生于 C++ PPU 仍链接在二进制内的构建（`build-rust-ppu/`，`FCEUX11_RUST_PPU:BOOL=ON`），即真正的"C++ 引擎侧"基线。

---

## 2. C++ 引擎侧 batch_compat 全量矩阵：现状与缺口

- **未持久化**：本仓库当前没有保存 3451 ROM 全量 batch_compat 矩阵文件（`docs/` / `tests/fixtures/` 内均无该产物）。M0 只记录了 Grade 与 9 项失败清单（见 §1 表格与 `v2.1.1.7_m0_baseline.md` §3）。
- **重跑成本**：全量一次约 3.6 h，plan §Step E gate 4 要求"owner 同意后"执行。
- **处置**：Step C 期间与收尾（M5/Step E）一律以 §1 的 Grade + 冻结基线 + ctest 逐项对齐为准；若 owner 需要"逐 ROM 字节级对照"，须在全量重跑前**先在本 tag 上跑一次并落盘**，否则删除 C++ PPU 后无法再生成 C++ 侧矩阵。这是本参照系唯一已知缺口，已在 plan §7.1 标记。

---

## 3. 遗留构建目录说明（C.3 的依据）

| 目录 | 状态 | 说明 |
|------|------|------|
| `build-rust-ppu/` | **唯一验证目录** | `CMakeCache` 内 `FCEUX11_RUST_PPU:BOOL=ON`；本 sub-plan 全部 gate 均在此目录执行 |
| `build/` | 历史遗留 | 缓存为 `FCEUX11_RUST_PPU=OFF`（AGENTS.md Gotcha 11）；`scripts/run_matrix.ps1` 曾硬编码该目录 |
| `build-ab-cpp/` | 历史 A/B 目录 | 用于 C++ PPU 侧对照构建；**Step C.2 之后 `FCEUX11_RUST_PPU=OFF` 变为 configure-time `FATAL_ERROR`，该目录不再可复现**。若需重建 C++ 引擎侧，必须 checkout 本 tag（`v2.1.1-cpp-ppu-reference`） |

---

## 4. 回滚路径

- Step C 之后若失效：`git revert` 对应 batch commit 即可恢复被删 TU；若已跨多个 batch，可 `git checkout v2.1.1-cpp-ppu-reference -- src/` 取回 C++ 引擎全量源码（配合 `src/CMakeLists.txt` 回滚）。
- 兼容壳恢复：把 C.2 的 configure-time `FATAL_ERROR` 改回 `option(FCEUX11_RUST_PPU ... ON)` + `add_definitions(-DFCEUX11_RUST_PPU)` 即可重新构建 C++ PPU 路径。

---

*本文件由 C.0（Step C 冻结参照）产出，是 M4/M5 期间判定"C++ 引擎侧是否退化"的对照入口。数据来源：2026-09-10~12 于 `wip2.1rc2` 上的实测记录与 `docs/history/v2.1.1.7_m0_baseline.md`。*