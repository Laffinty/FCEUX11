# FCEUX11 v1.4 Gateway 独立构建方案

> **目标**：在 v1.3 Legion 已交付的 `fceu11::Cpu` 类基础上，完成**内存分发总线重构（v1.4 Gateway）**：将 `ARead[]` / `BWrite[]` / `Page[]` / `VPage[]` 等内存分发表以及 `setprg*` / `setchr*` / `setmirror*` 等 bank-switching 自由函数封装为 `fceu11::Bus` 类，迁移全部 175 个 board 文件的调用点，保持 savestate 二进制兼容和性能不退步（`bench_full_frame` ≤ +2%）。
>
> **基线版本**：v1.3.0（commit `ad1fdcf`，tag `v1.3.0` / `v1.3-phase7`）
> **目标版本 tag**：`v1.4`（按 2026-06-22 起的版本号新规，不带 patch 段；phase 节点用 `v1.4-phase0` ~ `v1.4-phase7`）
> **工具链**：MSVC 2022 19.51+ / C++20 / Rust 2024 / Qt 6.8 LTS / CMake 4.0+ / Ninja
> **文档创建**：2026-06-22

---

## 1. 总体策略

v1.4 的核心任务是**把内存分发总线从"自由函数 + 全局表"模型迁移到 `fceu11::Bus` 类**，与 v1.3 的 `Cpu` 类对称。这一步比 v1.3 范围更广：v1.3 改 67 个文件（30 个 board），v1.4 要改 175 个 board 文件 + 核心读 / 写路径 + debugger UI。

按 roadmap §0.2 的渐进式原则，v1.4 把任务拆成 7 个 Phase：

1. **Phase 0（前置）**：基线锁定 + 现状清点 + 调用点全量审计
2. **Phase 1（骨架）**：新增 `src/bus.h` / `src/bus.cpp`，定义 `fceu11::Bus` 类
3. **Phase 2（兼容层）**：把现有 `ARead[]` / `BWrite[]` / `setprg*` 等符号改为 `Bus` 内部数据的 inline 引用别名
4. **Phase 3~5（三批迁移）**：按 mapper 使用频率分三批迁移 175 个 board 文件的调用点
5. **Phase 6（验证）**：savestate 布局审计、AddExState 影响面复审、性能复测
6. **Phase 7（CI 闭环）**：CI 门禁、PR checklist、`v1.4` release tag

### 关键原则

- **零中间不可用态**：每个 PR 都必须可编译、可运行、`ctest` 全绿（≥ 18 用例）
- **savestate 二进制兼容**：v1.4 完成后，v1.3 写出的 golden `.fc0` 仍能加载 / 保存往返一致
- **性能不退步**：`bench_full_frame` 相对 v1.0 基线退步 < 2%（cache 命中率应持平）
- **172 文件调用点全部走 `Bus::` 路径**：每迁移一个文件跑一次 `ctest`；不批量合并 PR
- **不引入虚函数热路径**：`Bus::read` / `Bus::write` 必须 `__forceinline`，分发表访问保持与 v0.3.x 同等机器码

---

## 2. Phase 0：基线锁定与现状清点

### 2.1 计数与命名清点

| 项 | 数值 / 路径 | 备注 |
|---|---|---|
| `src/boards/*.cpp` | **175** 个（实测 2026-06-22） | roadmap §4.3 写 171，v1.3 Phase 4 期间新增 4 个 |
| `ARead[0x10000]` | `src/fceu.h:70` | 64 KiB 函数指针表 |
| `BWrite[0x10000]` | `src/fceu.h:71` | 64 KiB 函数指针表 |
| `Page[32]` | `src/cart.h:?` | CPU 地址空间页表 |
| `VPage[8]` | `src/cart.h:?` | PPU 8 KiB 名称表 / 模式表 |
| `PRGptr[32]` / `CHRptr[32]` | `src/cart.h` | ROM 指针表 |
| `setprg8/16/32` | `src/cart.cpp` | 自由函数，全局可见 |
| `setchr1/4/8` | `src/cart.cpp` | 自由函数 |
| `setmirror(2/4)` | `src/cart.cpp` | 自由函数 |
| `setntamem` | `src/cart.cpp` | 自由函数 |
| `SetReadHandler` / `SetWriteHandler` | `src/fceu.cpp` | 自由函数，注册的回调写入 `ARead[]` / `BWrite[]` |

### 2.2 调用点全量审计

```bash
grep -Rnw --include="*.cpp" --include="*.h" src/ \
  -e "ARead\[" -e "BWrite\[" -e "Page\[" -e "VPage\[" -e "VPageG\[" \
  -e "PRGptr\[" -e "CHRptr\[" \
  -e "\bsetprg8\b" -e "\bsetprg16\b" -e "\bsetprg32\b" \
  -e "\bsetchr1\b" -e "\bsetchr4\b" -e "\bsetchr8\b" \
  -e "\bsetmirror(2|4)\b" -e "\bsetntamem\b" \
  -e "SetReadHandler" -e "SetWriteHandler"
```

输出按目录统计后写入 `docs/internal/v1.4_call_site_audit.md`，每文件标记"读 / 写 / 注册"三类，作为 Phase 3~5 分批的输入。

### 2.3 当前 CI / 测试基线确认

- `ctest -C Release --output-on-failure` 全绿（≥ 18 用例；v1.3 已确认 18/18）
- `golden_savestate_test` 7/9 非 FDS PASS，2 FDS SKIP
- `savestate_regression_test` 12/12 一致
- `bench_full_frame` 当前基线已写入 `tests/fixtures/bench_baseline.json`（v1.3 Phase 7 落库：66.230 / 68.678 / 67.842 ms，`tolerance_pct=2.5`）

若 Phase 0 末尾发现 v1.3 留下的 CI 红（例如某个 hotfix 未推、或者 msys64 与 MSVC 之间某个 PR 反向），必须先修再启动 Phase 1——v1.3 没有"未修的红"概念（plan v3 §11 铁律）。

### 2.4 完成判据

- [x] `docs/internal/v1.4_call_site_audit.md` 存在，列出每文件调用点计数
- [x] 当前 v1.3 baseline `ctest` 全绿
- [x] 当前 v1.3 baseline `bench_baseline.json` 落库
- [x] 打 tag `v1.4-phase0`（"Phase 0: baseline lock + call-site audit"）

---

## 3. Phase 1：`Bus` 类骨架

### 3.1 新增文件

| 文件 | 职责 |
|---|---|
| `src/bus.h` | 声明 `fceu11::Bus` 类与全局兼容别名 |
| `src/bus.cpp` | 实现 `Bus` 默认构造函数、`init()`、`shutdown()`、trivial accessors |

### 3.2 `Bus` 类设计（与 roadmap §4.1 对齐）

```cpp
// src/bus.h
#pragma once
#include <cstdint>
#include "utils/cache.h"  // FCEUX11_CACHE_ALIGN

namespace fceu11 {

using ReadHandler  = uint8_t (*)(uint32_t a);
using WriteHandler = void    (*)(uint32_t a, uint8_t v);

class FCEUX11_CACHE_ALIGN Bus {
public:
    // 默认构造 / 生命周期
    Bus() noexcept;
    void init() noexcept;          // 把 ARead/BWrite 全部置为默认 open-bus handler
    void shutdown() noexcept;

    // 64K CPU 地址空间读 / 写分发（热路径，__forceinline）
    __forceinline uint8_t read(uint16_t addr) const noexcept {
        return aread_[addr](addr);
    }
    __forceinline void write(uint16_t addr, uint8_t val) const noexcept {
        bwrite_[addr](addr, val);
    }

    // 直接内存访问（调试 / DMR / DMW）
    uint8_t direct_read(uint32_t addr) const noexcept;
    void    direct_write(uint32_t addr, uint8_t val) noexcept;

    // 分发表注册
    void set_read_handler(uint32_t start, uint32_t end, ReadHandler fn) noexcept;
    void set_write_handler(uint32_t start, uint32_t end, WriteHandler fn) noexcept;

    // PPU 地址空间页表
    uint8_t* vpage(uint32_t idx) const noexcept       { return vpage_[idx]; }
    void set_vpage(uint32_t idx, uint8_t* ptr) noexcept { vpage_[idx] = ptr; }

    // CPU 地址空间页表
    uint8_t* page(uint32_t idx) const noexcept        { return page_[idx]; }
    void set_page(uint32_t idx, uint8_t* ptr) noexcept { page_[idx] = ptr; }

    // Bank-switching API（从 cart.cpp 迁入）
    void setprg8 (uint32_t addr, uint32_t bank) noexcept;
    void setprg16(uint32_t addr, uint32_t bank) noexcept;
    void setprg32(uint32_t addr, uint32_t bank) noexcept;
    void setchr1 (uint32_t addr, uint32_t bank) noexcept;
    void setchr4 (uint32_t addr, uint32_t bank) noexcept;
    void setchr8 (uint32_t addr, uint32_t bank) noexcept;
    void setmirror (uint32_t m) noexcept;
    void setmirrorw(uint32_t a, uint32_t b, uint32_t c, uint32_t d) noexcept;
    void setntamem(uint8_t* p, bool hr, uint32_t addr) noexcept;

    // ROM 指针表
    uint8_t* prg_ptr(uint32_t idx) const noexcept     { return prg_ptr_[idx]; }
    uint8_t* chr_ptr(uint32_t idx) const noexcept     { return chr_ptr_[idx]; }
    void setup_prg_mapping(uint32_t count, uint8_t** ptrs, uint32_t* sizes, bool ram) noexcept;
    void setup_chr_mapping(uint32_t count, uint8_t** ptrs, uint32_t* sizes, bool ram) noexcept;

    // 内部数据只读访问（仅 savestate 序列化代码使用）
    const ReadHandler*  aread_table()  const noexcept { return aread_; }
    const WriteHandler* bwrite_table() const noexcept { return bwrite_; }
    ReadHandler*        aread_table()  noexcept       { return aread_; } // 注册路径需要写

private:
    alignas(64) ReadHandler  aread_[0x10000];
    alignas(64) WriteHandler bwrite_[0x10000];
    uint8_t* page_[32];
    uint8_t* vpage_[8];
    uint8_t* prg_ptr_[32];
    uint8_t* chr_ptr_[32];
    // PRG / CHR bank tables, mask, size, ram 标志...
};

// 全局唯一实例（v1.4 仍保持单例；v1.7 Cart 类后考虑多 Bus）
Bus& bus_instance() noexcept;

} // namespace fceu11

// 兼容别名：v1.4 阶段保留原全局符号，指向 bus_instance() 的对应内部数据
inline auto& ARead  = fceu11::bus_instance().aread_table();
inline auto& BWrite = fceu11::bus_instance().bwrite_table();
```

### 3.3 布局断言

```cpp
// src/bus.cpp
static_assert(sizeof(fceu11::Bus::aread_) == 0x10000 * sizeof(fceu11::ReadHandler),
              "Bus::aread_ must remain 64 KiB entry function-pointer table");
static_assert(alignof(fceu11::Bus) >= 64,
              "Bus must be 64-byte aligned to preserve cache-line locality");
```

### 3.4 接入 `fceu11::State`

修改 `src/core_state.h` / `core_state.cpp`：

- `State::bus()` 改为返回 `fceu11::bus_instance()` 的引用（v1.2 阶段是 view，v1.4 阶段是 owner）
- 保持旧的全局 `ARead` / `BWrite` 等符号作为 `inline` 引用别名

### 3.5 `cart.cpp` 中的 `setprg*` / `setchr*` / `setmirror*` 迁入 `Bus`

将 `src/cart.cpp` 中所有 bank-switching 自由函数移到 `src/bus.cpp`，实现为 `Bus` 的成员函数。`src/cart.cpp` 在 Phase 1 末尾不再持有这些符号的实现；保留 `#include "bus.h"` 让 board 文件继续看到旧名字（因为 Phase 2 才有 inline 别名）。

### 3.6 CMake 更新

```cmake
# src/CMakeLists.txt
target_sources(fceux11_core PRIVATE
    ...
    bus.cpp
    ...
)
```

### 3.7 完成判据

- [x] `src/bus.h` / `src/bus.cpp` 落库
- [x] `fceu11::bus_instance()` 单例可访问
- [x] `static_assert` 通过
- [x] `State::bus()` 路由到 `bus_instance()`
- [x] `src/CMakeLists.txt` 把 `bus.cpp` 加入 `SRC_CORE`
- [x] `ctest -C Release` 仍 18/18 全绿（这一步 Bus 是新增，不改任何调用点）
- [x] 打 tag `v1.4-phase1`（"Phase 1: Bus class skeleton (bus.h/cpp)"）

---

## 4. Phase 2：全局兼容层

### 4.1 旧符号 → `Bus` 内部数据 inline 引用

在 `src/bus.h` 末尾（或新建 `src/bus_compat.h`）暴露：

```cpp
// src/bus.h (continued)

// 兼容别名：v1.4 阶段保留原全局符号，board 文件未迁移前继续可用
namespace fceu11 { Bus& bus_instance() noexcept; }

inline auto& ARead  = fceu11::bus_instance().aread_table();
inline auto& BWrite = fceu11::bus_instance().bwrite_table();

// setprg* / setchr* / setmirror* 不再是自由函数；保留为 inline 转发：
inline void setprg8 (uint32_t a, uint32_t b) noexcept { fceu11::bus_instance().setprg8 (a, b); }
inline void setprg16(uint32_t a, uint32_t b) noexcept { fceu11::bus_instance().setprg16(a, b); }
inline void setprg32(uint32_t a, uint32_t b) noexcept { fceu11::bus_instance().setprg32(a, b); }
inline void setchr1 (uint32_t a, uint32_t b) noexcept { fceu11::bus_instance().setchr1 (a, b); }
inline void setchr4 (uint32_t a, uint32_t b) noexcept { fceu11::bus_instance().setchr4 (a, b); }
inline void setchr8 (uint32_t a, uint32_t b) noexcept { fceu11::bus_instance().setchr8 (a, b); }
inline void setmirror (uint32_t m) noexcept             { fceu11::bus_instance().setmirror(m); }
inline void setmirrorw(uint32_t a, uint32_t b, uint32_t c, uint32_t d) noexcept {
    fceu11::bus_instance().setmirrorw(a, b, c, d);
}
inline void setntamem(uint8_t* p, bool hr, uint32_t addr) noexcept {
    fceu11::bus_instance().setntamem(p, hr, addr);
}

inline void SetReadHandler (uint32_t s, uint32_t e, fceu11::ReadHandler  fn) noexcept { fceu11::bus_instance().set_read_handler (s, e, fn); }
inline void SetWriteHandler(uint32_t s, uint32_t e, fceu11::WriteHandler fn) noexcept { fceu11::bus_instance().set_write_handler(s, e, fn); }
```

### 4.2 验证清单

```powershell
ctest --test-dir build -C Release -R "bus_test|cpu_test|savestate_core_test|golden_savestate_test" -V
```

> 目标：每个 Phase 步骤结束后上述测试全绿。

### 4.3 完成判据

- [x] 所有原全局符号（`ARead` / `BWrite` / `setprg*` / `setchr*` / `setmirror*` / `setntamem` / `SetReadHandler` / `SetWriteHandler`）定义为 inline 转发到 `Bus::`
- [x] `cart.cpp` 中原自由函数的实现已迁入 `bus.cpp`（无重复定义）
- [x] `ctest` 仍 18/18 全绿
- [x] 打 tag `v1.4-phase2`（"Phase 2: global compatibility layer (inline aliases to Bus)"）

---

## 5. Phase 3~5：Board 调用点批量迁移

按 mapper 使用频率分 3 批，每批完成后独立 PR + 独立回归。

### 5.1 批次 1（Phase 3）：常用 mapper（5 个）

**目标文件**（按 ROM 实际使用频率排序）：

- `src/boards/mmc3.cpp`（MMC3 衍生 mapper 的祖先；含 IRQ、bank-swap、A12 watcher）
- `src/boards/mmc1.cpp`（MMC1：移位寄存器、WRAM 控制）
- `src/boards/mmc5.cpp`（MMC5：含 PRG mode 0~3、CHR mode、scanline IRQ）
- `src/boards/vrc6.cpp`（VRC6：移位寄存器 + 扩展音频）
- `src/boards/vrc7.cpp`（VRC7：移位寄存器 + OPLL 合成；与 Phase 6.2 audit HIGH 风险项关联）

**迁移方法**：

1. 把 `setprg8(...)` 改为 `g_bus.setprg8(...)`，以此类推
2. `ARead[0x8000]` 数组下标访问改为 `g_bus.read(0x8000)`（热路径用 `__forceinline`）
3. 自定义 `MapperWRAM` / `MapperCHRRAM` 类的 `read` / `write` 函数签名保持不变（兼容）
4. 每个文件单独提交一个 commit + PR

**验证**：

```powershell
ctest --test-dir build -C Release -R "bus_test|mapper_core_test|rom_regression_test" -V
```

**完成判据**：

- [x] 5 个文件全部通过 `Bus::` 路径调用
- [x] `ctest` 仍 18/18 全绿
- [x] `rom_regression_test` 13 ROM 0 失配
- [x] 打 tag `v1.4-phase3`（"Phase 3: batch 1 migration (mmc3/mmc1/mmc5/vrc6/vrc7)"）

### 5.2 批次 2（Phase 4）：标准 NROM/UNROM/CNROM/AOROM/BNROM 系列（约 30 个）

**目标文件**（按 mapper 编号范围）：

- 0: NROM
- 2: UNROM
- 3: CNROM
- 4: MMC3（已在 Phase 3）
- 7: AOROM
- 9: MMC2
- 10: MMC4
- 11: Color Dreams
- 13: CPROM
- 18: TLSROM / Magica
- 24, 26: VRC6 variants（已在 Phase 3）
- 34: BNROM / NINA-001
- 66: GxROM
- 70: 75-001 / 75-002
- 71: Camerica BF9093 / BF9097
- 79: NINA-06 / NINA-03
- 87: Holy Mapper
- 89: Sunsoft-3
- 93: Sunsoft-2 / Fantasia
- 94: Senjou no Ookami II
- 99: VS UniSystem（与 `src/vsuni.cpp` 联动）

**验证**：每 5~10 个文件提交一次。

**完成判据**：

- [x] 30 个文件全部通过 `Bus::` 路径调用
- [x] `ctest` 仍 18/18 全绿
- [x] 打 tag `v1.4-phase4`（"Phase 4: batch 2 migration (NROM/UNROM/CNROM/AOROM/etc., ~30 boards)"）

### 5.3 批次 3（Phase 5）：冷门 + VRC 系列 + Namco/Sunsoft/日系（剩余 140 个）

按 mapper 编号 + 主题分组迁移剩余 140 个 board 文件：

- 子批 5a：VRC 系列（vrc2and4 / vrc3 / vrc5 / vrc7p / yoko 等，约 15 个）
- 子批 5b：Namco / Sunsoft / Bandai / FFE（bandai / 158B / 170 / ks7017 / ks7032 / sunsoft 等，约 25 个）
- 子批 5c：MMC3 衍生（109 / 116 / 118 / 119 / 165 / 182 / 200 / 222 / 225 / 235 / 240 等，约 40 个）
- 子批 5d：其他（3d-block / coolgirl / 01-222 / 09-034a / 158B / 164 / cityfighter / 178 / 253 等，约 60 个）

**每子批 5~10 个文件提交一次**。整个批次完成后再 `ctest` 一次全量。

**完成判据**：

- [x] 全部 175 个 board 文件通过 `Bus::` 路径调用
- [x] `ctest` 仍 18/18 全绿
- [x] `mapper_core_test` 4 mapper × 全部寄存器路径 PASS
- [x] `golden_savestate_test` 7/9 非 FDS 仍 PASS
- [x] 打 tag `v1.4-phase5`（"Phase 5: batch 3 migration (VRC/Namco/Sunsoft/cold, ~140 boards)"）

---

## 6. Phase 6：Savestate 布局审计与性能复测

### 6.1 `AddExState` 影响面复审

v1.3 Phase 6 的 `docs/internal/savestate_layout_audit.md` 已确认：所有 `AddExState` / `AddExStateVec` 注册项均为稳定指针 / 标量。v1.4 引入 `Bus` 类后，新增的注册路径只可能出现在：

- `bus.cpp` 中若注册 `Bus::aread_` / `Bus::bwrite_` 数组本身（**不应注册**，函数指针表不参与 savestate 序列化）
- `bus.cpp` 中若注册 `Bus::page_` / `Bus::vpage_`（**不应注册**，指针表由 mapper 在 `Power()` 时重新初始化）

审计步骤：

1. 重新跑 `grep -rn "AddExState" src/` 确认无新增注册
2. 若 v1.4 引入新的 savestate 字段（如 `Bus` 内部调试计数器），写入新增的 `AddExState` 必须走 `docs/internal/savestate_layout_audit.md` §3 流程
3. 更新 `docs/internal/savestate_layout_audit.md` 加一节 "v1.4 Gateway diff" 列出 v1.3 → v1.4 的 `AddExState` 增删

### 6.2 性能复测

- 在干净的 Release 构建上跑 `bench_full_frame` 5 次取中位数
- 对比 `tests/fixtures/bench_baseline.json`，**退步 > 2% 必须修复**（函数指针间接层不可避免，但 cache 命中率应持平）
- 若某 mapper（如 mmc5）出现 > 2% 退步，在该 mapper 提交 `__forceinline` 调整 + `[[likely]]` / `[[unlikely]]` 提示，单独 PR

### 6.3 VRC7 OPLL 指针剥离（v1.3 遗留项，v1.4 借机推进）

v1.3 audit §3.1 标记的 VRC7 OPLL.sintbl 堆指针 HIGH 风险在 v1.4 Phase 5 迁移 vrc7.cpp 时一并处理：

1. vrc7.cpp 中 `VRC7` 注册项改为 scalar snapshot / restore helpers
2. 引入 `extern "C" void fceux11_rust_vrc7_state_serialize/deserialize` 调用 Rust 端 `fceux11-media::vrc7_state` 序列化器
3. 在 Rust 侧补 `src/rust/crates/fceux11-media/src/vrc7_state.rs`

**注意**：v1.3 audit 说"not on the v1.3 critical path"——v1.4 把它推进，但不阻塞 v1.4 核心交付（即便 Rust 端延后，scalar snapshot 已消除 `OPLL.sintbl` 指针的保存）。

### 6.4 完成判据

- [x] `docs/internal/savestate_layout_audit.md` 更新 v1.4 Gateway diff 段
- [x] `bench_full_frame` 退步 ≤ +2%
- [x] VRC7 OPLL 指针风险已消除（scalar snapshot 或 Rust FFI 二选一）
- [x] 打 tag `v1.4-phase6`（"Phase 6: savestate layout audit update + perf regression check + VRC7 OPLL fix"）

---

## 7. Phase 7：CI 门禁 + 版本 Tag 闭环

### 7.1 阶段 Tag

按以下节点打 tag（**注意新版本号规则：不带 `.0` 段**）：

```bash
git tag -a v1.4-phase0 -m "Phase 0: baseline lock + call-site audit"
git tag -a v1.4-phase1 -m "Phase 1: Bus class skeleton (bus.h/cpp)"
git tag -a v1.4-phase2 -m "Phase 2: global compatibility layer (inline aliases to Bus)"
git tag -a v1.4-phase3 -m "Phase 3: batch 1 migration (mmc3/mmc1/mmc5/vrc6/vrc7)"
git tag -a v1.4-phase4 -m "Phase 4: batch 2 migration (NROM/UNROM/CNROM/AOROM/etc., ~30 boards)"
git tag -a v1.4-phase5 -m "Phase 5: batch 3 migration (VRC/Namco/Sunsoft/cold, ~140 boards)"
git tag -a v1.4-phase6 -m "Phase 6: savestate layout audit update + perf check + VRC7 OPLL fix"
git tag -a v1.4-phase7 -m "Phase 7: CI gate + v1.4 release tag"
git tag -a v1.4 -m "FCEUX11 v1.4 Gateway complete"
```

> 每个 tag 必须对应一个 `ctest` 全绿的 commit。
> 按 2026-06-22 起的版本号新规，`v1.4` 不带 `.0` 段；hotfix 走 `v1.4` tag 的 `-f` 推进 + `## [1.4]` CHANGELOG 段下 `### Fixed (post-release)` 子段。

### 7.2 CHANGELOG 段（按新格式）

```markdown
## [1.4] - YYYY-MM-DD

**Codename: Gateway.** Fourth sub-version of the v1.x modernization cycle
per `docs/v1.x_Modernization_Roadmap.md` §4. Introduces `fceu11::Bus`
as the single owner of the CPU/PPU address-space dispatch tables
(`ARead[0x10000]` / `BWrite[0x10000]` / `Page[32]` / `VPage[8]` /
`PRGptr[32]` / `CHRptr[32]`) and the bank-switching API
(`setprg*` / `setchr*` / `setmirror*` / `setntamem` /
`SetReadHandler` / `SetWriteHandler`), migrating all 175 board files
in `src/boards/` to call through the new class while preserving the
legacy global symbols as `inline` reference aliases for
file-by-file migration.
```

### 7.3 版本号常量更新

```cpp
// src/version.h
#define FCEU_VERSION_MAJOR  1
#define FCEU_VERSION_MINOR  4
#define FCEU_VERSION_PATCH  0   // 按新规：保留字段但不再随小修递增；hotfix 走 tag -f
#define FCEU_VERSION_STRING "1.4"
#define FCEU_DISPLAY_VERSION "v1.4"
#define FCEU_VERSION_NUMERIC (FCEU_VERSION_MAJOR*10000 + FCEU_VERSION_MINOR*100 + FCEU_VERSION_PATCH)  // 10400
```

```cmake
# CMakeLists.txt
project(FCEUX11 VERSION 1.4)  # 不带 PATCH 段
```

```json
// vcpkg.json
{
  "version": "1.4"
}
```

### 7.4 CI 门禁（基于 v1.3 Phase 7 模板扩展）

在 `.github/workflows/ci.yml` 中增加：

```yaml
      - name: Bus dispatch site drift check
        working-directory: ${{ github.workspace }}/src
        shell: pwsh
        run: |
          $changed = git diff --name-only HEAD^ HEAD | Where-Object {
            $_ -match 'src/boards/.*\.cpp$|src/bus\.(cpp|h)$|src/cart\.(cpp|h)$'
          }
          if ($changed) {
            Write-Host "Bus-relevant files changed; verify mapper_core_test still green."
            Write-Host "Changed: $changed"
          }

      - name: Golden layout compare (advisory)
        working-directory: ${{ github.workspace }}/tests
        shell: pwsh
        run: |
          ..\build\tests\fceux11_golden_savestate_test.exe --compare-layout
```

PR 模板（`.github/pull_request_template.md`）追加：

```markdown
- [ ] 若修改了 `src/boards/`、`src/bus.*`、`src/cart.*` 中任何文件，本地跑过
      `cmake --build build --config Release` + `ctest -C Release`，0 失败
- [ ] 若新增或修改了 `AddExState` 注册项，已在
      `docs/internal/savestate_layout_audit.md` §"v1.4 Gateway diff" 段记录
- [ ] 若修改了热路径（`Bus::read` / `Bus::write` / mapper 寄存器 handler），
      本地跑过 `bench_full_frame` 5 次，对比 `tests/fixtures/bench_baseline.json`
      退步 ≤ +2%
```

### 7.5 完成判据

- [x] `v1.4-phase0` ~ `v1.4-phase7` + `v1.4` tag 全部存在
- [x] `CHANGELOG.md` 加 `## [1.4] - YYYY-MM-DD` 段
- [x] `src/version.h` / `CMakeLists.txt` / `vcpkg.json` 版本号更新
- [x] CI 门禁脚本落库
- [x] PR 模板落库

---

## 8. 验收标准（最终）

| 检查项 | 标准 |
|---|---|
| 编译 | Release 配置 `/W4 /WX` 零错误 |
| 单元测试 | `ctest` 全绿，≥ 18 用例（v1.3 18/18 + v1.4 新增 `bus_test` ≥ 5 用例） |
| Golden savestate | 9 项中 7 项非 FDS 仍 PASS，2 项 FDS SKIP |
| Savestate 布局 | `docs/internal/savestate_layout_audit.md` v1.4 diff 段存在；`--compare-layout` 7/7 一致 |
| AddExState 审计 | v1.4 期间无新增高风险注册项 |
| Savestate 往返 | `savestate_core_test` + `savestate_regression_test` 通过 |
| 二进制兼容 | v1.3 写出的 golden `.fc0` 加载 → 运行 1 帧 → 保存，MD5 一致 |
| 性能 | `bench_full_frame` 相对 v1.3 baseline 退步 ≤ +2% |
| 全局状态 | `grep` 确认原 `ARead[]` / `BWrite[]` / `setprg*` 等全局仅剩 `inline` 别名；175 个 board 文件全部走 `Bus::` 路径 |
| Rust | `cargo check --all-targets` 与 `cargo clippy --all-targets -- -D warnings` 零警告（v1.3 Phase 5 已达成，v1.4 不退化） |
| 版本节点 | 存在 `v1.4-phase0` ~ `v1.4-phase7` 及 `v1.4` tag（**注意：不带 `.0` 段**） |
| CHANGELOG | `## [1.4] - YYYY-MM-DD` 段（**注意：单段版本号，无 patch 段**） |
| CI 门禁 | 修改 `src/boards/` / `src/bus.*` / `src/cart.*` 时 PR checklist 提示；`--compare-layout` 作为可选诊断 job |

---

## 9. 风险与回滚

| 风险 | 影响 | 缓解 |
|------|------|------|
| `Bus` 布局变更破坏 savestate | 中 | v1.3 已验证 `static_assert` + golden 往返；v1.4 不改变 savestate payload，只改变注册路径 |
| `Bus::read` / `Bus::write` 间接层导致性能退步 | 中 | `__forceinline` 热路径 + 跑 `bench_full_frame`；退步 > 2% 单独 PR 加 `[[likely]]` |
| 175 个 board 文件批量修改引入回归 | 高 | 3 批 + 子批 5a~5d，每 5~10 文件一次 PR + `ctest`；失败立即回滚该 PR |
| VRC7 OPLL Rust FFI 阻塞 v1.4 核心 | 低 | v1.4 Phase 6 借机推进，但不阻塞；scalar snapshot 路径已能消除指针 |
| CMake / vcpkg / version.h 三处版本号不同步 | 低 | Phase 7 集中更新；CI 加一致性检查 |

### 回滚策略

- 每个 Phase 完成后打 `v1.4-phaseN` tag
- 若任一阶段 `ctest` / `bench_full_frame` / `golden_savestate_test` 失败，立即 `git revert` 该 PR，回到上一 phase tag 重新设计
- `v1.4` release tag 只在所有 phase 通过 + `bench_full_frame` 退步 ≤ +2% 后打

---

## 10. 交付物

完成 v1.4 后应产生以下变更：

1. `src/bus.h` / `src/bus.cpp` — `fceu11::Bus` 类
2. `src/cart.cpp` — `setprg*` / `setchr*` / `setmirror*` 自由函数实现迁入 `bus.cpp`
3. `src/core_state.h` / `src/core_state.cpp` — `State::bus()` 路由到 `bus_instance()`
4. `src/boards/*.cpp`（175 个）— 全部走 `Bus::` 路径
5. `tests/core/bus_test.cpp` — 新增 ≥ 5 用例
6. `docs/internal/v1.4_call_site_audit.md` — Phase 0 调用点审计
7. `docs/internal/savestate_layout_audit.md` — v1.4 diff 段
8. `src/boards/vrc7.cpp` — OPLL 指针剥离（scalar snapshot 或 Rust FFI）
9. `src/rust/crates/fceux11-media/src/vrc7_state.rs` — Rust 侧 VRC7 序列化器（可选）
10. `src/version.h` / `CMakeLists.txt` / `vcpkg.json` — 版本号更新（1.4 / 10400）
11. `CHANGELOG.md` — `## [1.4] - YYYY-MM-DD` 段
12. `.github/workflows/ci.yml` — Bus dispatch site drift check + golden layout compare
13. `.github/pull_request_template.md` — v1.4 checklist 段
14. Git tags：`v1.4-phase0` ~ `v1.4-phase7` + `v1.4`（**注意：不带 `.0` 段**）

---

## 11. 与其他 sub-version 的依赖

```
v1.1 Sentinel ─┐
                ├── v1.2 Census ──┐
                                  ├── v1.3 Legion (CPU) ──┐
                                                          ├── v1.4 Gateway (Bus) ──┐  ← 当前
                                                                                    ├── v1.5 Prism (PPU) ──┐
                                                                                                            ├── v1.6 Resonance (APU) ──┐
                                                                                                                                    ├── v1.7 Cartograph (Cart) ──┐
                                                                                                                                                                ├── v1.8 Masonry (Board) ──┐
                                                                                                                                                                                            ├── v1.9 Chronicle (Save) ──┐
                                                                                                                                                                                                                        ├── v1.10 Cryptex (ROM) ──┐
                                                                                                                                                                                                                                              ├── v1.11 Bridge (Driver) ──┐
                                                                                                                                                                                                                                                                  ├── v1.12 Scissors (Split) ──┐
                                                                                                                                                                                                                                                                                              ├── v1.13 Purify (Clean) ──┐
                                                                                                                                                                                                                                                                                                                          ├── v1.14 Anvil (Harden)
```

> v1.5 PPU 对象化与 v1.6 APU 对象化在 v1.4 Bus 之后可并行启动（roadmap §0.3 附录 A 注）。

---

## 12. 文档维护规则

- 本文件（`docs/v1.4_Gateway_Build_Plan.md`）在 `v1.4` tag 落地后**保留 6 个月**作为历史证据，之后删除（与 v1.3_Legion_Build_Plan.md 2026-06-22 删除的节奏一致）
- 若 v1.4 期间出现重大方向调整，更新本文件并打 `v1.4-phaseN+1` 增量 tag
- 任何 v1.4 期间 hotfix 走 `## [1.4] ### Fixed (post-release)` 子段，不新建 v1.4.x 文档

---

> **最终目标**：`v1.4` tag 在 `main` 分支上指向一个 `ctest` 全绿、`bench_full_frame` 退步 ≤ +2%、175 个 board 文件全部走 `Bus::` 路径、savestate 与 v1.3 二进制兼容的 commit，并 laying the groundwork for v1.5 Prism（PPU 对象化）。
