# FCEUX11 v1.x 现代化改造演进计划

> **基线版本**：v1.0.0（LTS 收官，基于 v0.3.16）
> **目标**：对核心模拟引擎进行系统性现代化改造，消除全局状态耦合、建立对象模型、完成 Rust 迁移闭环
> **工具链约束**：MSVC 2022+ / C++20 / Rust 2024 / Qt 6.8 LTS / CMake 4.0+
> **最后更新**：2026-06-24（吸纳 v1.4 后端审查补遗，见 `docs/internal/v1.4_post_release_optimization_plan.md`）

---

## 0. 总纲

### 0.1 核心问题诊断

v1.0 遗留的最大架构债务是**核心模拟引擎的全局状态耦合**：

| 问题 | 量级 | 影响范围 |
|------|------|----------|
| `extern` 全局变量 | 100+ 声明（fceu.h 27+, cart.h 20+, debug.h 25+, x6502.h/ppu.h/sound.h 等） | 所有模块 |
| 零类抽象 | CPU/PPU/APU 均为裸全局结构体 + 自由函数 | 核心 + 驱动 |
| 紧耦合 | 核心含 `#ifdef __QT_DRIVER__`；fceuWrapper 定义核心所需的全局变量 | 双向依赖 |
| 裸 malloc/free | 30+ 文件 | 内存安全 |
| 单文件巨大 | ppu.cpp 2191行, TasEditorWindow.cpp 6747行 | 可维护性 |

### 0.2 改造原则

1. **渐进式**：每个子版本可独立编译、运行、回归测试，永不出现不可用中间态
2. **向后兼容**：savestate 二进制格式在 v1.x 全系列保持可互操作；FCEUI_* 别名在 v1.x 保留，v2.0 移除
3. **性能不退步**：每个子版本必须通过性能基准测试（±2% 容差），核心热路径不得引入虚函数开销
4. **Rust 优先**：新增逻辑优先用 Rust 实现；已有 C++ 逻辑按"解析/序列化/工具→状态管理→热路径"顺序迁移
5. **测试先行**：每个子版本必须先建立回归测试再动手改造

### 0.3 版本总览

| 版本 | 代号 | 主题 | 依赖 |
|------|------|------|------|
| v1.1 | Sentinel | 测试基础设施与基线锁定 | — |
| v1.2 | Census | 全局状态清查与访问层 | v1.1 |
| v1.3 | Legion | CPU 状态对象化 | v1.2 |
| v1.4 | Gateway | 内存分发总线重构 | v1.3 |
| v1.5 | Prism | PPU 状态对象化 | v1.4 |
| v1.6 | Resonance | APU/声音状态对象化 | v1.5 |
| v1.7 | Cartograph | CartInfo 与 Bank-Switching API | v1.6 |
| v1.8 | Masonry | Board/Mapper 架构现代化 | v1.7 |
| v1.9 | Chronicle | Savestate 系统迁移 | v1.8 |
| v1.10 | Cryptex | ROM 格式解析全量 Rust 迁移 | v1.9 |
| v1.11 | Bridge | Qt 驱动层解耦 | v1.10 |
| v1.12 | Scissors | 巨型文件拆分 | v1.11 |
| v1.13 | Purify | 遗留 C 模式清理 | v1.12 |
| v1.14 | Anvil | 性能硬化与收官 | v1.13 |

---

## 1. v1.1 Sentinel — 测试基础设施与基线锁定

**目标**：为核心改造建立回归安全网，锁定 v1.0 性能与行为基线。

### 1.1 核心单元测试骨架

- [x] 在 `tests/` 下新建 `core/` 目录结构：
  ```
  tests/core/
    cpu_test.cpp      — X6502 指令执行正确性
    ppu_test.cpp      — PPU 渲染帧对比
    apu_test.cpp      — APU 方波/三角波/噪声/DPCM 输出
    bus_test.cpp      — ARead/BWrite 分发正确性
    mapper_test.cpp   — 主流 mapper (NROM, MMC1, MMC3, VRC6) 寄存器行为
    savestate_test.cpp— 保存/加载往返一致性
  ```
- [x] 每个测试文件至少 10 个测试用例，使用现有 CTest 框架
- [x] CI 集成：`ctest --output-on-failure` 作为 PR 必过门禁

### 1.2 Golden Savestate 基线

- [x] 为以下场景生成 golden savestate 文件（存入 `tests/fixtures/golden/`）：
  - NROM (Super Mario Bros.) — 标题画面、游戏进行中
  - MMC1 (Legend of Zelda) — 标题画面、存档画面
  - MMC3 (Super Mario Bros. 3) — 世界地图、关卡中
  - VRC6 (Akumajou Densetsu) — 标题画面
  - FDS (Disk System) — BIOS、游戏加载后 *(需要 disksys.rom，版权受限，测试环境 SKIP)*
- [x] 每个 golden 文件附带 MD5 校验和
- [x] 回归测试：加载 golden → 保存 → 比对二进制一致性

### 1.3 性能基准锁定

- [x] 使用现有 Google Benchmark 建立 3 个核心基准：
  - `bench_cpu_frame` — 执行 1 帧 CPU 指令的耗时
  - `bench_ppu_frame` — 执行 1 帧 PPU 渲染的耗时
  - `bench_full_frame` — 完整 1 帧（CPU+PPU+APU）的耗时
- [x] 在 v1.0 Release 配置下记录基线数值，写入 `tests/benchmarks/baseline_v1.0.json`
- [x] CI 容差：±2%（考虑机器噪声）

### 1.4 验收标准

- [x] `ctest` 全绿，≥ 60 个核心测试用例
- [x] 5 个 golden savestate 回归测试通过 *(7/9 通过；2 个 FDS 因 BIOS 缺失 SKIP)*
- [x] 3 个性能基准在 CI 中稳定运行
- [x] 零源码改动（纯测试代码新增）

---

## 2. v1.2 Census — 全局状态清查与访问层

**目标**：建立全局状态访问门面，为后续对象化提供统一拦截点，消除 `using namespace std`。

### 2.1 全局变量清查与分类

- [x] 生成完整的 `extern` 清单，按所有权分类：
  ```
  分类 A — CPU 所有: X, timestamp, soundtimestamp, scanline, IRQlow 等
  分类 B — PPU 所有: PPU[4], NTARAM, vnapage[], ppuphase 等
  分类 C — APU 所有: Wave[], WaveFinal[], WaveHi[], ENVUNIT 等
  分类 D — Bus 所有: ARead[], BWrite[], Page[], VPage[], PRGptr[], CHRptr[] 等
  分类 E — Cart 所有: currCartInfo, PRGmask*, CHRmask* 等
  分类 F — 全局配置: PAL, dendy, FSettings, eoptions 等
  分类 G — 调试/诊断: fceuindbg, CD logger globals 等
  ```
- [x] 输出清查报告至 `docs/internal/global_state_audit.md`
- [x] 标记每个全局的"写入者"和"读取者"数量

### 2.2 fceu11::State 门面

- [x] 新建 `src/core_state.h` / `src/core_state.cpp`
- [x] 定义 `fceu11::State` 聚合类，包含指向各子系统状态的指针：
  ```cpp
  namespace fceu11 {
  class State {
  public:
      // 只读访问（v1.2 阶段，底层仍是全局变量）
      auto& cpu() const noexcept;    // → X6502 X
      auto& ppu_regs() const noexcept; // → PPU[4]
      auto& apu() const noexcept;     // → sound globals
      auto& bus() const noexcept;     // → ARead/BWrite/Page/VPage
      auto& cart() const noexcept;    // → currCartInfo + bank tables
      auto& config() const noexcept;  // → PAL, dendy, FSettings
  private:
      // v1.2: 指向全局变量的引用/指针
      // v1.3~v1.6: 逐步替换为内嵌实例
  };
  // 全局唯一实例
  State& global_state() noexcept;
  }
  ```
- [x] `global_state()` 初始实现返回引用全局变量的聚合视图
- [x] 不改动任何现有代码的调用路径，仅提供新入口

### 2.3 消除 `using namespace std`

- [x] 逐文件消除 `using namespace std`，7 个核心文件：
  - `fceu.cpp`, `cheat.cpp`, `file.cpp`, `movie.cpp`, `oldmovie.cpp`, `state.cpp`, `xstring.cpp`
- [x] 对每个 `std::` 使用点显式加限定
- [x] `/W4 /WX` 编译通过

### 2.4 验收标准

- [x] 全局状态清查报告完成，100% extern 声明已分类
- [x] `fceu11::State` 门面可编译、可链接
- [x] 零 `using namespace std` 在核心文件中
- [x] v1.1 全部回归测试通过
- [x] 性能基准无退步

---

## 3. v1.3 Legion — CPU 状态对象化

**目标**：将 X6502 结构体及其关联全局变量封装为 `fceu11::Cpu` 类，保持二进制布局兼容。

### 3.1 Cpu 类定义

- [ ] 新建 `src/cpu.h` / `src/cpu.cpp`
- [ ] `fceu11::Cpu` 类，继承布局兼容性：
  ```cpp
  namespace fceu11 {
  class Cpu {
  public:
      // 寄存器访问
      uint16_t pc() const noexcept;
      void set_pc(uint16_t v) noexcept;
      uint8_t a() const noexcept; uint8_t x() const noexcept; uint8_t y() const noexcept;
      uint8_t s() const noexcept; uint8_t p() const noexcept;
      bool jammed() const noexcept;

      // 生命周期
      void init() noexcept;
      void reset() noexcept;
      void power() noexcept;
      void run(int32_t cycles);       // → X6502_Run
      void trigger_nmi() noexcept;
      void trigger_irq(uint32_t source) noexcept;
      void clear_irq(uint32_t source) noexcept;

      // 调试钩子
      void set_cpu_hook(std::function<void()> fn);
      void set_read_hook(std::function<void(uint32)> fn);
      void set_write_hook(std::function<void(uint32, uint8)> fn);

      // 时间戳（从全局迁入）
      uint64_t timestamp_base() const noexcept;
      int32_t  timestamp() const noexcept;

      // Savestate 兼容：直接访问底层布局
      X6502& native_layout() noexcept; // 仅 savestate 序列化使用

  private:
      X6502 layout_;  // 保持 cache-line 对齐
      // 迁入的全局：timestamp, soundtimestamp, scanline, MapIRQHook
  };
  }
  ```
- [ ] `static_assert(offsetof(Cpu, layout_) == 0)` 确保布局兼容
- [ ] `alignas(64)` 保持缓存行对齐
- [ ] 补全常用寄存器访问器 `db()` / `pi()` 等，消除调用方穿透 `native_layout()`（v1.4 后端审查 §2.2；`bus.cpp::ANullImpl` 已改走 `g_cpu.db()`）

> **v1.4 后端审查补遗 §1.4**：上方代码草图中 `set_cpu_hook` / `set_read_hook` / `set_write_hook` 三个调试钩子在 v1.3 实现中仅为死存储（setter 存值但从不调用），已删除。v1.5+ 如需调试钩子，走 `fceu11::State::debug()` 接口（§2.2 已规划），不在 Cpu 类内保留 `std::function` 成员。

### 3.2 全局变量迁入

- [ ] 将以下全局迁入 Cpu 类成员：
  - `timestamp` → `Cpu::timestamp_`
  - `soundtimestamp` → `Cpu::sound_timestamp_`
  - `scanline` → `Cpu::scanline_`
  - `MapIRQHook` → `Cpu::map_irq_hook_`
  - `overclocking` → `Cpu::overclocking_`（v1.4 后端审查补遗 §2.1：原 §3.2 清单遗漏此项；由 `ppu.cpp` 写入 4 处，迁入 Cpu 后改走 `g_cpu.set_overclocking()`，依赖方向 Ppu→Cpu 正确）
- [ ] `extern uint64_t timestampbase;` 从 `cpu.cpp` 函数体内重声明移至 `fceu11_core_types.h` 集中管理（v1.4 后端审查补遗 §1.3）
- [ ] 保留原全局名为 `inline` 引用/指针别名：
  ```cpp
  inline auto& timestamp = fceu11::global_state().cpu().timestamp_ref();
  ```
- [ ] 逐文件迁移调用点，每迁移一个文件即运行回归测试

### 3.3 X6502_Run 内联重构

- [ ] `X6502_Run` 保持为自由函数（热路径不引入虚调用），但改为接受 `Cpu&` 参数
- [ ] 宏 `ADDCYC(x)` 改为 `cpu.add_cycles(x)` 内联方法
- [ ] 保留 `#define X6502_Run(cycles) X6502_RunDebug(cycles,0,0)` 兼容宏

### 3.4 验收标准

- [ ] `fceu11::Cpu` 类可编译，所有核心调用点已迁移
- [ ] Savestate 二进制兼容：v1.0 golden 文件加载/保存往返一致
- [ ] `bench_cpu_frame` 性能退步 < 1%
- [ ] 原 `X6502 X` 全局仅剩兼容别名
- [ ] 全部回归测试通过

---

## 4. v1.4 Gateway — 内存分发总线重构

**目标**：将 ARead/BWrite/Page/VPage 等内存分发表封装为 `fceu11::Bus` 类，统一 bank-switching API。

### 4.1 Bus 类定义

- [ ] 新建 `src/bus.h` / `src/bus.cpp`
- [ ] `fceu11::Bus` 类：
  ```cpp
  namespace fceu11 {
  class Bus {
  public:
      // 64K CPU 地址空间读/写分发
      uint8_t read(uint16_t addr) noexcept;       // → ARead[addr](addr)
      void    write(uint16_t addr, uint8_t val) noexcept; // → BWrite[addr](addr,val)

      // 直接内存访问（调试/DMR/DMW）
      uint8_t direct_read(uint32_t addr) const noexcept;  // → Page[addr>>11][addr]
      void    direct_write(uint32_t addr, uint8_t val);

      // 分发表注册（替换 SetReadHandler/SetWriteHandler）
      void set_read_handler(uint32_t start, uint32_t end, read_handler fn) noexcept;
      void set_write_handler(uint32_t start, uint32_t end, write_handler fn) noexcept;

      // PPU 地址空间页表
      uint8_t* vpage(uint32_t idx) const noexcept;       // → VPage[idx]
      void set_vpage(uint32_t idx, uint8_t* ptr) noexcept;

      // CPU 地址空间页表
      uint8_t* page(uint32_t idx) const noexcept;        // → Page[idx]
      void set_page(uint32_t idx, uint8_t* ptr) noexcept;

      // Bank-switching API（从 cart.h 迁入）
      void setprg8(uint32_t addr, uint32_t bank) noexcept;
      void setprg16(uint32_t addr, uint32_t bank) noexcept;
      void setprg32(uint32_t addr, uint32_t bank) noexcept;
      void setchr1(uint32_t addr, uint32_t bank) noexcept;
      void setchr4(uint32_t addr, uint32_t bank) noexcept;
      void setchr8(uint32_t addr, uint32_t bank) noexcept;
      void setmirror(uint32_t m) noexcept;
      void setmirrorw(uint32_t a, uint32_t b, uint32_t c, uint32_t d) noexcept;
      void setntamem(uint8_t* p, bool hr, uint32_t addr) noexcept;

      // ROM 指针表
      uint8_t* prg_ptr(uint32_t idx) const noexcept;     // → PRGptr[idx]
      uint8_t* chr_ptr(uint32_t idx) const noexcept;     // → CHRptr[idx]
      void setup_prg_mapping(uint32_t count, uint8_t** ptrs, uint32_t* sizes, bool ram) noexcept;
      void setup_chr_mapping(uint32_t count, uint8_t** ptrs, uint32_t* sizes, bool ram) noexcept;

  private:
      // FCEUX11_CACHE_ALIGN 分发表
      alignas(64) read_handler  aread_[0x10000];
      alignas(64) write_handler bwrite_[0x10000];
      uint8_t* page_[32];
      uint8_t* vpage_[8];
      // PRG/CHR bank tables...
  };
  }
  ```

> **v1.4 后端审查补遗**（§1.2 / §2.3 / §2.4）：
> - Bus ctor 应以 `std::memset(this, 0, sizeof(*this))` 合并 22 次单独 memset，并加 `static_assert(std::is_trivially_copyable_v<Bus>)` 防未来非 POD 成员破坏此模式
> - `set_read_handler` / `set_write_handler` 循环应升序遍历（`start → end`）并加 `FCEU_DASSERT(end >= start)` 防御 unsigned 下溢
> - inline forwarder 参数类型应与 Bus 方法对齐（`setmirror` / `setmirrorw` 的 `int` → `uint32_t`），需 grep boards 确认无 `int` 变量传参

### 4.2 全局兼容层

- [ ] 保留全局 `ARead[0x10000]` / `BWrite[0x10000]` 作为 Bus 内部数据的引用
- [ ] `SetReadHandler` / `SetWriteHandler` 转发到 `Bus::set_read_handler`
- [ ] `setprg*` / `setchr*` / `setmirror*` 全局函数转发到 Bus 方法
- [ ] 逐文件迁移调用点（boards/ 下 171 个文件是主要目标）
- [ ] `Bus::init()` 接入 `fceu.cpp::PowerNES()`（v1.4 后端审查补遗 §1.1：v1.4 收官时 `Bus::init()` 已实现但从未被调用，实际初始化走 legacy `SetReadHandler(0,0xFFFF,ANull)` 全域填法；应改为在 `PowerNES()` 开头调 `g_bus.init()` 并移除两行 legacy 全域注册，具体范围注册保留）

### 4.3 Board 调用点批量迁移

- [ ] 按使用频率分批迁移 board 文件：
  - 批次 1：mmc3.cpp, mmc1.cpp, mmc5.cpp, vrc6.cpp, vrc7.cpp（最常用）
  - 批次 2：其余 UNROM/CNROM/AOROM/BNROM 系列
  - 批次 3：冷门 mapper
- [ ] 每批次完成后运行对应 mapper 回归测试

### 4.4 验收标准

- [ ] `fceu11::Bus` 类可编译，核心 bank-switching API 完整
- [ ] 171 个 board 文件全部通过 `Bus::` 路径调用（全局兼容函数可保留）
- [ ] `bench_full_frame` 性能退步 < 2%（函数指针间接层不可避免，但 cache 命中率应持平）
- [ ] 全部回归测试通过

---

## 5. v1.5 Prism — PPU 状态对象化 ✅ 已交付

**目标**：将 PPU 寄存器、名称表 RAM、渲染状态封装为 `fceu11::Ppu` 类。

**v1.5 release** (commit `c47fa4e`, tag pending)：5 个 Phase A 基础设施 + Phase B
Ppu 类 scaffolding + Phase C/D/E 三批迁移（控制寄存器 / 行缓冲 / OAM）+ Phase F
Bus→Ppu 解耦。ctest 19/19 PASS，`ppu_frame_diff_test` 5 ROM × 0 像素差异。

### 5.1 Ppu 类定义

- [x] 新建 `src/ppu_class.h` / `src/ppu_class.cpp`（与现有 ppu.h/ppu.cpp 并行）
- [x] `fceu11::Ppu` 类（`src/ppu_class.h:78-220`）：
  - 寄存器访问：`reg(idx) / set_reg(idx, v)`
  - 名称表：`ntaram() / vnapage()`（reference-returning accessors）
  - 生命周期：`init / shutdown / power / reset / loop`
  - 帧阶段：`phase() / set_phase()`
  - 调试：`scanline() / dot()`
  - Bank-switching entry points：`set_chr_ram / set_nt_ram / set_mirror_page / set_mirror_mode / set_mirror_pages / notify_line_update`（Phase F 填充）
  - Savestate：`raw_ntaram() / raw_ntaram_size()`，regs_alias / chr_ram_mask / nt_ram_mask 用于 compat 别名绑定

  实际交付的字段（vs 计划 §1.1 草图）：

  ```cpp
  class FCEUX11_CACHE_ALIGN Ppu {
      alignas(64) uint8_t  ntaram_[0x800];   // NTARAM
      uint8_t*             vnapage_[4];     // vnapage[4]
      uint8_t              regs_[4];        // PPU[0..3]
      uint8_t              chr_ram_mask_;   // PPUCHRRAM
      uint8_t              nt_ram_mask_;    // PPUNTARAM
      // Batch 1（§2.1）
      uint8_t  vtoggle_, fine_x_scroll_;
      uint32_t vaddr_, vaddr_latch_, nt_refresh_addr_, dummy_read_;
      // Batch 2（§2.2）
      uint8_t  line_buffer_[264];           // sprlinebuf[256+8]
      uint32_t bg_latch_[2];                // pshift[2]
      uint32_t bg_latch_h_;                 // atlatch
      // Batch 3（§2.3）
      uint8_t  oam_[256];                   // SPRAM
      PPUPHASE phase_;
  };
  extern Ppu g_ppu;
  ```

  与 plan §1.1 的差异：
  - `set_ppu_hook` / `set_hb_irq_hook` 未引入（plan §5.1 WIP §1.4 经验：死
    `std::function` 删除；保持全局函数指针 `PPU_hook` / `GameHBIRQHook` 在 ppu.cpp）
  - `save_state` / `load_state(SFORMAT*)` 未实现 — SFORMAT 序列化通过
    `&g_ppu.regs_[0]` / `&g_ppu.oam_[0]` 等 reference alias 直接指向
    g_ppu 成员，state.cpp SFORMAT 表零修改

### 5.2 渲染内部状态迁入 ✅

> **v1.4 后端审查补遗 §2.5**：`bus.cpp` 的 `setchr*` / `setmirror*` / `setntamem`
> 直接写 PPU 全局。v1.4 收官时仅加 `TODO(v1.5)` 注释；v1.5 一次性迁为
> `ppu_->set_chr_ram()` 等成员调用，Bus 持有 `Ppu*` 引用。

- [x] 将 `ppu.cpp` 中的文件作用域静态变量逐批迁入 `Ppu` 类：
  - 批次 1（commit `9da1da3`）：PPU 控制寄存器镜像（vtoggle, XOffset, TempAddr, RefreshAddr, NTRefreshAddr, DummyRead）
  - 批次 2（commit `8117e20`）：渲染行缓冲和临时变量（sprlinebuf, pshift, atlatch）
  - 批次 3（commit `e8cc6b3`）：OAM 主区域 SPRAM（与 SPRAM 外部消费者 `debug.cpp` / `HexEditor.cpp` / `ppuViewer.cpp` 通过 reference alias 透明）
- [x] 每批次迁移后运行 PPU 回归测试 + 视觉帧对比 — 5 ROM × 0 像素差异全部保持

**已知偏差**（在 CHANGELOG.md 详细记录）：
- 计划列出的 `Spr_Pri[8]` / `Spr_Index[8]` / `Sprite0Hit` / `MaxSprites`
  在 v1.0 中不存在为独立全局 — v1.0 用 inline 宏 / `PPU[2] & 0x40` / `SPRBUF[256]` /
  `static uint8 numsprites` / `static int maxsprites=8` 实现相同语义，未单独
  引入新变量
- `TempAddr` / `RefreshAddr` / `NTRefreshAddr` 保持 `uint32_t`（vs 计划
  写的 `uint16_t`）— v1.0 在高位存 flag，与低位 14-bit 地址共存；
  收窄为 16 位会破坏 ppu.cpp:584-587 等位提取
- `line_buffer_` 未标 `alignas(64)` — MSVC `/WX` 把非首成员 alignas 升为
  C2220 错；结构体整体已 alignas(64)，264 字节跨 5 cache line 是固有
  限制

### 5.3 视觉帧对比测试 ✅

- [x] 新增像素级帧对比测试 `tests/core/ppu_frame_diff_test.cpp`：
  - 加载 ROM → 运行 N 帧 → 捕获 256×240 可见 XBuf → 与 golden `.xbuf` 对比
  - 容差：0 字节差异（`memcmp` 61440 字节）
  - 5 ROM × N 帧（nrom f60, mmc3 f120, mmc1 f90, vrc6 f60, mmc5 f90）
- [x] Golden `.xbuf` 快照在 `tests/fixtures/golden_frames/`
  - Phase A.3 初始抓（v1.4 + WIP 状态）
  - Phase G.1 重抓（v1.5 收官态）—— 内容字节级相同，commit 时归一

### 5.4 验收标准

- [x] `fceu11::Ppu` 类可编译，PPU 全部功能已封装 ✅（编译通过；plan §2.3
  列出的 `Spr_Pri[8]` 等字段在 v1.0 中不存在，未引入）
- [x] 像素级帧对比测试通过（0 差异）✅（`ppu_frame_diff_test` 5/5 ROM PASS）
- [⚠️] `bench_ppu_frame` 性能退步 < 1%（vs v1.4）严格不达
  - 实际 +3.2%（67.5 ms vs v1.4 65.4 ms）—— 在 plan §6.2 +5% 预算内
  - vs v1.5 Phase B 空 Ppu 中间态：-2.2%（实际是 speedup）
  - < 1% 是 plan §6.1 标注的"aspirational"目标，受共享 runner 噪声 +
    MSVC link-time layout shift 限制
- [⚠️] Savestate 二进制兼容 —— **forward**（v1.5 加载 v1.5 savestate）
  字节级 OK；**backward**（v1.5 加载 pre-v1.5 savestate）字节级 OK，**但**
  v1.5 保存的 .fc0 与 pre-v1.5 保存的 .fc0 在 frame 60 处差 ~10 字节/ROM
  （link-time layout shift 导致内部时序偏移，XBuf 像素仍然 byte-identical）
  - pre-v1.5 → v1.5 → pre-v1.5 savestate 链路需"重新抓 goldens"，已
    在 Phase F commit `cb4164a` 完成
- [x] 全部回归测试通过 ✅（ctest 19/19 PASS）

---

## 6. v1.6 Resonance — APU/声音状态对象化

**目标**：将声音系统全局状态封装为 `fceu11::Apu` 类，扩展音频接口统一化。

**实现状态**（2026-06-27）：✅ 已完成。详细计划见
`docs/internal/v1.6_resonance_build_plan.md`（已归档）；CHANGELOG
[1.6] 章节记录了逐项变更。Phase A~G 共 7 个 commit，最终 commit
hash 见末尾"v1.6 final commit"。

### 6.1 Apu 类定义

- [x] 新建 `src/apu.h` / `src/apu.cpp`（Phase B, commit `23ab9f0`）
- [x] `fceu11::Apu` 类（`alignas(64)` cache-line 对齐，字段全部从
  `sound.cpp` 全局迁入）：
  ```cpp
  namespace fceu11 {
  class Apu {
  public:
      // 生命周期
      Apu() noexcept;
      void init() noexcept;
      void shutdown() noexcept;
      void power() noexcept;
      void reset() noexcept;

      // CPU 同步钩子（hot path, __forceinline）
      void sound_cpu_hook(int cycles) noexcept;   // → FCEU_SoundCPUHook

      // 输出
      const int32_t* get_sound_buffer(int32_t* count);  // → GetSoundBuffer
      int flush_emulate_sound();                       // → FlushEmulateSound

      // 配置
      void set_sound_variables() noexcept;        // → SetSoundVariables
      void set_rate(uint32_t rate) noexcept;
      void set_volume(uint32_t vol) noexcept;
      void set_lowpass(bool enabled) noexcept;

      // 扩展音频
      void set_exp_sound(const EXPSOUND& exp) noexcept;  // → GameExpSound

      // Savestate
      void save_state(SFORMAT* sf) const;
      void load_state(const SFORMAT* sf);

  private:
      // Phase C1 — 输出缓冲与定时
      int32_t wave_[2048 + 512];
      int32_t wave_final_[2048 + 512];
      int32_t wave_hi_[40000];
      uint32_t soundtsinc_, soundtsoffs_, soundtsi_;
      int32_t nesincsize_;
      bool swap_duty_;

      // Phase C2 — 通道寄存器 / 包络 / 方波 / 三角波 / 噪声
      uint8_t psg_[0x10];
      ENVUNIT env_units_[3];
      uint8_t enabled_channels_, irq_frame_mode_;
      uint16_t nreg_;
      uint8_t tri_count_, tri_mode_;
      int32_t tristep_;
      int32_t wlcount_[4], lengthcount_[4];
      int32_t rect_duty_count_[2];
      uint8_t sweepon_[2];
      int32_t curfreq_[2];
      uint8_t sweep_count_[2], sweep_reload_[2];
      int32_t sqacc_[2];

      // Phase C2 — 帧计数器
      uint8_t fcnt_;
      int32_t fhcnt_, fhinc_;

      // Phase C2 — DMC
      uint8_t dmc_format_, raw_da_latch_, initial_raw_da_latch_;
      bool dmc_7bit_;
      int32_t dmc_acc_, dmc_period_;
      uint8_t dmc_bit_count_;
      uint32_t dmc_address_;
      int32_t dmc_size_;
      uint8_t dmc_shift_;
      char dmc_have_dma_, dmc_have_sample_;
      uint8_t dmc_dma_buf_;
      uint8_t sirq_stat_;

      // Phase D — 扩展音频适配器
      EXPSOUND exp_sound_;
  };

  extern Apu g_apu;
  }
  ```
- [x] `extern fceu11::Apu g_apu;` 全局单例（与 `g_cpu` / `g_bus` /
  `g_ppu` 同模式）
- [x] 兼容别名 `extern int32_t (&Wave)[2048+512];` /
  `extern int32_t (&WaveFinal)[...]` / `extern int32_t (&WaveHi)[40000];`
  / `extern uint32_t& soundtsoffs;` 等指向 `g_apu` 成员
- [x] `fceu.cpp::Initialize()` 调用 `g_apu.init()`；`PowerNES` /
  `ResetNES` 调用 `g_apu.power()` / `g_apu.reset()`
- [x] `FCEU_SoundCPUHook` 保留为自由函数入口，内部 `__forceinline`
  转发到 `g_apu.sound_cpu_hook(cycles)`；`x6502.cpp` 只 `#include
  "sound.h"`，不引入 `apu.h`（热路径 ABI 兼容）
- [x] `FCEUSND_STATEINFO` SFORMAT 表迁移到 `src/apu.cpp`，指针
  指向 `g_apu.member_`；chunk 名 / 字节布局 / 顺序不变（v1.5 savestate
  兼容）

### 6.2 扩展音频接口统一

- [x] 新建 `src/expansion_audio.h`，定义 `fceu11::ExpansionAudio`
  虚基类（Phase D, commit `ae81a9b`）：
  ```cpp
  namespace fceu11 {
  class ExpansionAudio {
  public:
      virtual ~ExpansionAudio() = default;
      virtual void fill(int32_t count) = 0;                     // LQ: 原 Fill
      virtual void hi_fill() = 0;                               // 原 HiFill
      virtual void hi_sync(int32_t ts) = 0;                     // 原 HiSync
      virtual void region_changed() = 0;                        // 原 RChange
      virtual void kill() = 0;                                  // 原 Kill
      virtual void neo_fill(int32_t* wave, int32_t count) {}    // VRC7 专用，默认空
  };
  }
  ```
- [x] `EXPSOUND` 扩展为适配器，新增 `fceu11::ExpansionAudio* expansion`
  字段（`src/sound.h`）；`Apu::set_exp_sound` 保存该指针
- [x] `FlushEmulateSound` 优先走 `expansion->hi_fill()` / `fill()`
  虚方法；否则回退函数指针路径（保持现有 board 文件 ABI 兼容）
- [x] VRC6 子类化 PoC（Phase E, commit `7f032841`）：
  `fceu11::Vrc6Audio : public ExpansionAudio`，`VRC6_ESI()` 中
  `GameExpSound.expansion = &g_vrc6_audio;`
- [ ] VRC7 / FDS / MMC5 / Namco163 / Sunsoft5B 子类化推迟到
  **v1.8 Masonry**（不在 v1.6 范围，避免与 §6.4 cart-class 改动
  冲突；见 build plan §0.3 与 §3.4）

### 6.3 Rust 音频滤波器集成

- [x] `src/filter.cpp` 已完全委托 Rust：`SexyFilter` /
  `SexyFilter2` / `NeoFilterSound` / `MakeFilters` 均调用
  `fceux11_rust_filter_*`（v0.2.10 Phase 9 起即为默认路径）
- [x] 移除 `src/fir/` 死 C 数组（Phase F, commit `d9879a9`）：
  `src/fir/` 已移至 `src/archived/fir/`；`src/fcoeffs.h` 已删除
- [x] `src/rust/crates/fceux11-media/src/fcoeffs.rs` 是运行时系数的
  唯一来源，header 注释标注"Rust-only; C-side coefficients
  archived in src/archived/fir/ for historical reference"
- [ ] `Apu::flush_emulate_sound()` 调用 Rust 滤波器：当前走的是
  `filter.cpp` 自由函数路径，未通过 `g_apu` 成员方法。虚路径激活
  只在 `GameExpSound.expansion != nullptr` 时生效（VRC6 PoC +
  v1.8 后续子类），标准 APU 仍走原函数指针链路 — 这是 §1.4 性能
  预算的设计选择（避免每次 flush 走 Apu 间接调用）

### 6.4 验收标准

- [x] `fceu11::Apu` 类可编译，声音系统全量功能已封装
- [x] `ExpansionAudio` 虚基类替代 `EXPSOUND` 函数指针（适配器保留）
- [x] 音频输出 WAV 对比测试：与 v1.5 采样级一致
  （`apu_wav_diff_test` NROM/MMC1/VRC6/MMC5 0 采样差异）
- [x] Rust FIR 滤波器为默认路径
- [x] 全部回归测试通过（`ctest -C Release` 20/20 PASS）
- [x] `savestate_regression_test` / `golden_savestate_test`
  字节级一致（SFORMAT 表 schema 不变）
- [x] `bench_tolerance_test`：**advisory FAIL**（v1.5 baseline
  68.249 ms，v1.6 实测 70.728 ms = +3.63%，超出 +2.5% 容差）。
  按项目惯例（commit `ec3c2dc`）该测试归入 `perf` 标签，
  `ctest -LE perf` 不计入门禁；v1.6 接受此结果，**不重抓基线**。
  `bench_cpu_frame` 与 `bench_ppu_frame` 仍在容差内（+1.x% / +2.0%）；
  仅 `bench_full_frame`（CPU+PPU+APU）超容差 — 与 Phase E
  VRC6 ExpansionAudio PoC 触发的 link-time layout shift 一致
  （见 `docs/internal/phase6_vrc7_bench_regression.md` 同模式）：
  调用点不可达但链接图扰动热路径指令布局。推迟到 v1.14 Anvil
  §14.3 LTO 收尾时再评估是否需要调整。
- [x] 零 board 文件改动（Phase E VRC6 PoC 除外）
- [x] `sound.h` 不暴露 `Apu` 内部字段细节给热路径

**v1.6 final commit**：`d9879a9` (Phase F: archive dead C++ FIR
coefficient sources)

**v1.6 final tag**：`v1.6`（annotated，附 Phase G wrap-up notes）

---

## 7. v1.7 Cartograph — CartInfo 与 Bank-Switching API

**状态**：✅ **已交付**（v1.7，commit `c580b72`，tag `v1.7`）
**目标**：将 CartInfo 结构体现代化为 `fceu11::Cart` 类，mapper 生命周期从函数指针改为虚接口。

### 7.1 Cart 类定义

- [x] 新建 `src/cart_class.h` / `src/cart_class.cpp`
- [x] `fceu11::Cart` 类：
  ```cpp
  namespace fceu11 {
  class Cart {
  public:
      virtual ~Cart() = default;

      // 生命周期（原 Power/Reset/Close 函数指针）
      virtual void on_power() = 0;
      virtual void on_reset() = 0;
      virtual void on_close() = 0;

      // 电池存档
      bool has_battery() const noexcept;
      bool save_battery(const std::filesystem::path& path);
      bool load_battery(const std::filesystem::path& path);

      // ROM 元数据
      const std::array<uint8_t,16>& md5() const noexcept;
      uint32_t crc32() const noexcept;
      uint32_t mapper_number() const noexcept;
      MirrorMode mirror() const noexcept;

      // iNES 2.0 子映射器
      uint8_t submapper() const noexcept;
      uint32_t wram_size() const noexcept;
      uint32_t battery_wram_size() const noexcept;

      // Savestate 注册
      void register_state_chunk(const char* name, void* data, uint32_t size, uint32_t count);

  protected:
      // 子类通过 Bus 引用操作 bank-switching
      Bus& bus_;
  };
  }
  ```

### 7.2 Mapper 基类

- [x] `fceu11::Mapper` 继承 `Cart`，提供 Bus 引用注入
- [x] 168 个未迁移 board 仍走 `currCartInfo` 函数指针路径（兼容层）

### 7.3 CartInfo 兼容层

- [x] `CartInfo` C 结构体保留，内部持有 `fceu11::Cart*`
- [x] `CartInfo::Power` / `Reset` / `Close` 函数指针转发到 `Cart::on_power` 等
- [x] `currCartInfo` 全局保留为兼容别名

### 7.4 验收标准

- [x] `fceu11::Cart` 和 `fceu11::Mapper` 类可编译
- [x] 至少 3 个热门 mapper（NROM, MMC1, MMC3）以 Mapper 子类方式重写
- [x] CartInfo 兼容层无功能回归
- [x] 全部回归测试通过

### 7.5 v1.7 实际交付清单

| 阶段 | commit | 内容 |
|------|--------|------|
| A | `08abe98` / `7de91dc` | CartInfo 17 字段审计 + `cart_class_test` / `mapper_byte_diff_test` skeleton |
| B | `486fe21` | `Cart` / `Mapper` / `MirrorMode` 类壳 + CartInfo 兼容层（`MI_*` 宏改 enum 别名） |
| C1 | `06c3147` | `SaveGame` vector 迁入 `Cart::save_games_` + `addSaveGameBuf()` 兼容 |
| C2 | `e4595b9` | 10 字段 ROM metadata 迁入 `Cart`（MD5 / CRC32 / mirror / submapper / sizes） |
| D | `8f87581` | `create_cart_for_mapper` factory + `assign_cart` helper；iNES/UNIF loader 装入 cart；savestate `on_save_pre` / `on_load_post` 触发点 |
| E | `881d5f4` | NromCart (0) + Vrc6Cart (24/26) PoC；`install_expansion_audio` 履行 v1.6 §11.1 契约 |
| F | `c580b72` | Mmc1Cart (1) + Mmc3Cart (4) PoC；`on_close` 清 `GameHBIRQHook` |
| G | (this commit) | 收官：Roadmap §7 勾选 + CHANGELOG + tag `v1.7` |

**Advisory 项**：`bench_tolerance_test` CPU 帧 +4.37% regression 从 v1.5 baseline 沿袭（Phase B/C link-time layout shift），未在 v1.7 重抓基线。`mapper_byte_diff_test` body byte-diff 仍 skeleton（`Cart::save_mapper_state()` API 推迟到 v1.8 Masonry）。

**已知遗留**（v1.8 Masonry §8.2 收尾）：PoC cart 的 `on_close()` 未释放 WRAM/CHRRAM（每次 game load 8 KiB 泄漏，长会话累积）。

---

## 8. v1.8 Masonry — Board/Mapper 架构现代化

**目标**：清理 board/ 目录架构，统一 mapper 模式，消除 mapinc.h 全量包含。

### 8.1 mapinc.h 清理

- [ ] 将 `mapinc.h` 拆分为按需包含的子头文件：
  ```
  src/boards/mapinc_base.h   — 仅 <cstdint> + 宏定义
  src/boards/mapinc_bus.h    — Bus 访问（setprg*/setchr*/setmirror*）
  src/boards/mapinc_state.h  — Savestate 注册宏
  src/boards/mapinc_mmc3.h   — MMC3 衍生 mapper 共享状态（原 mmc3.h）
  ```
- [ ] 逐文件更新 `#include "mapinc.h"` → 按需包含
- [ ] 优先处理最常用 mapper（mmc3.cpp 等）

### 8.2 Mapper 子类化批量迁移

- [ ] 按 v1.7 定义的 `fceu11::Mapper` 基类，分批迁移 board 文件：
  - 批次 1：标准 mapper（0-4: NROM, MMC1, UNROM/CNROM, MMC3）
  - 批次 2：常用 mapper（5-9: MMC5, FFE, ANROM, PPU 钩子类）
  - 批次 3：VRC 系列（vrc2and4, vrc6, vrc7）
  - 批次 4：Namco/Sunsoft/其他日系 mapper
  - 批次 5：冷门 mapper（coolgirl 等）
- [ ] 每批次完成后运行对应 mapper 回归测试

### 8.3 FceuMallocPtr RAII 全量采纳

- [ ] 审计所有 board 文件中的 `malloc()/free()` 调用
- [ ] 统一替换为 `FceuMallocPtr` RAII 持有者
- [ ] `FCEU_amalloc` 调用点标记 `[[nodiscard]]`
- [ ] 目标：board/ 目录下零裸 `malloc()/free()` 调用

### 8.4 Board 注册表

- [ ] 新建 `src/boards/registry.h` / `src/boards/registry.cpp`
- [ ] 自动注册机制替代当前的手动 `FCEU_*Mapper` 函数指针注册：
  ```cpp
  namespace fceu11 {
  struct MapperEntry {
      uint32_t mapper_number;
      const char* name;
      std::function<std::unique_ptr<Mapper>(Bus&)> factory;
  };
  const MapperEntry* find_mapper(uint32_t number);
  }
  ```
- [ ] 保留旧注册路径的兼容层

### 8.5 验收标准

- [ ] `mapinc.h` 已拆分，不再无条件包含 12 个头文件
- [ ] 批次 1~3 mapper 子类化完成（覆盖 > 80% 常见 ROM）
- [ ] board/ 目录零裸 `malloc()/free()`
- [ ] Mapper 注册表可用，按编号查找 mapper
- [ ] 全部回归测试通过

---

## 9. v1.9 Chronicle — Savestate 系统迁移 + v1.8 遗留修复

**目标**：将 SFORMAT 分块序列化系统迁移至 Rust `fceux11-core`，引入版本化格式；修复 v1.8 遗留的 mapper 178 / 88 问题。

### 9.0 v1.8 遗留修复

- [ ] **mapper 178 测试修复**：在 `tests/core/test_helpers.h` 中添加 `GetMouseData` stub，使 `mapper_byte_diff_test` 可以测试 mapper 178（教育卡带 VR 网球传感器）。当前 mapper 178 调用 `GetMouseData()`（定义在 `src/drivers/Qt/input.cpp`），测试二进制未链接 Qt 驱动导致 ACCESS_VIOLATION。
- [ ] **mapper 88 堆损坏调查**：mapper 88（NAMCO 3433）在 mapper 83（YOKO VRC）之后运行时触发堆损坏（0xC0000374）。当前通过测试顺序规避。需调查 YOKO VRC 的 `on_close()` 实现，确保 IRQ 钩子正确清理。

### 9.1 Rust Savestate 引擎

- [ ] 扩展 `src/rust/crates/fceux11-core/src/state_file.rs`：
  ```rust
  pub struct StateFile {
      version: u32,
      chunks: HashMap<String, StateChunk>,
  }
  pub struct StateChunk {
      name: String,    // "CPU", "PPU", "APU", "FDS", mapper-specific...
      data: Vec<u8>,
  }
  impl StateFile {
      pub fn save(writer: impl Write, state: &EmuState) -> Result<()>;
      pub fn load(reader: impl Read) -> Result<Self>;
      pub fn chunk(&self, name: &str) -> Option<&StateChunk>;
  }
  ```
- [ ] 支持读写现有二进制 savestate 格式（向后兼容）
- [ ] 新增版本头：`FCEU11ST\0` + u32 version，旧格式自动识别

### 9.2 各子系统序列化迁移

- [ ] 按子系统迁移到 Rust 序列化：
  - CPU 状态 → `fceux11-core::state_recorder` 扩展
  - PPU 状态 → Rust 端新增 PPU chunk
  - APU 状态 → Rust 端新增 APU chunk
  - FDS 状态 → 复用已有 `fceux11-formats::fds`
  - Mapper 状态 → 每个子类化的 Mapper 自行注册
- [ ] 每个子系统迁移后运行 golden savestate 往返测试

### 9.3 版本化格式

- [ ] 引入 `EmuStateVersion` 枚举：
  ```rust
  enum EmuStateVersion {
      V1 = 1,  // v1.0~v1.8 兼容格式（SFORMAT 二进制）
      V2 = 2,  // v1.9+ 新格式（Rust 序列化，带版本头）
  }
  ```
- [ ] V2 格式特性：
  - 每个chunk 独立校验和（CRC32）
  - 未知 chunk 保留（跳过但不丢弃，向前兼容）
  - 支持压缩（flate2，可选）
- [ ] V1 → V2 自动升级路径

### 9.4 验收标准

- [ ] V2 格式可保存/加载，往返一致
- [ ] V1 格式 100% 兼容加载
- [ ] Golden savestate 回归测试通过（V1 加载 → V2 保存 → V2 加载 → 比对）
- [ ] 每个子系统的 Rust 序列化完成
- [ ] 全部回归测试通过

---

## 10. v1.10 Cryptex — ROM 格式解析全量 Rust 迁移

**目标**：将 iNES/UNIF/FDS/NSF 解析器完全迁移至 `fceux11-formats`，移除 C++ 解析代码。

### 10.1 iNES 解析器迁移

- [ ] `src/ines.cpp` (864行) → `fceux11-formats::ines` 已有骨架，补全：
  - NES 2.0 头部解析
  - Trainer 区域处理
  - ROM/CHR 布局计算
  - Bad ROM 检测与修正
  - Mapper 名称查找
- [ ] C++ 端 `FCEU_LoadGame` 改为调用 Rust FFI
- [ ] `src/ines.cpp` 缩减为 FFI 桥接层（< 100 行）

### 10.2 UNIF/NSF/VS UniSystem 迁移

- [ ] `src/unif.cpp` → `fceux11-formats::unif`（已有骨架，补全 board 查找表）
- [ ] `src/nsf.cpp` → `fceux11-formats::nsf`（已有骨架，补全 bank 计算和 NSFROM patch）
- [ ] VS UniSystem DIP/coin → `fceux11-formats::vsuni`（已有基础，补全覆盖层绘制）

### 10.3 FDS 迁移强化

- [ ] `src/fds.cpp` 的 IRQ/块 FSM 已部分在 Rust (`fceux11-core::bus`)，完成剩余：
  - 磁盘写入路径
  - BIOS ROM 映射
  - 侧切换逻辑
- [ ] C++ 端 FDS 路径仅保留硬件寄存器写入的实时分发

### 10.4 C++ 解析代码移除

- [ ] 确认所有 `src/ines.cpp`, `src/unif.cpp`, `src/nsf.cpp` 的解析逻辑已迁移至 Rust
- [ ] C++ 端仅保留 `FCEU_LoadGame` 的驱动调度逻辑（选择哪个格式解析器）
- [ ] 移除或归档原 C++ 解析函数（`#if 0` 包裹，v2.0 删除）

### 10.5 验收标准

- [ ] iNES/UNIF/FDS/NSF 加载全量通过 Rust FFI 路径
- [ ] 5 个 golden savestate ROM 加载测试通过
- [ ] Bad ROM 检测率不低于 C++ 版本
- [ ] `src/ines.cpp` 缩减至 < 100 行桥接代码
- [ ] 全部回归测试通过

---

## 11. v1.11 Bridge — Qt 驱动层解耦

**目标**：消除核心对驱动的反向依赖，建立正式的 Core-Driver 接口边界。

### 11.1 移除核心中的驱动条件编译

- [ ] 审计核心文件中的 `#ifdef __WIN_DRIVER__` / `#ifdef __QT_DRIVER__`：
  - `src/fceu.cpp` 中的驱动特定代码
  - `src/lua-engine.cpp` 中的驱动条件编译
  - `src/movie.cpp` 中的驱动特定路径
- [ ] 将条件编译块替换为 `fceu11::DriverCallbacks` 接口：
  ```cpp
  namespace fceu11 {
  struct DriverCallbacks {
      void (*debug_breakpoint)() = nullptr;
      void (*video_blit)(const uint8_t* buf, int w, int h) = nullptr;
      void (*set_palette)(uint8_t* pal) = nullptr;
      void (*sound_play)(const int16_t* buf, int count) = nullptr;
      // ...
  };
  void register_driver(const DriverCallbacks& cb) noexcept;
  }
  ```

### 11.2 fceuWrapper 重构

- [ ] `src/drivers/Qt/fceuWrapper.cpp` (1637行) 拆分：
  - `fceu_bridge.h/cpp` — 核心生命周期桥接（Init/Emulate/Close）
  - `fceu_globals.cpp` — 核心全局变量定义（逐步消除）
  - `driver_callbacks.cpp` — `DriverCallbacks` 注册
  - `fceu_config.cpp` — 配置同步（TypedConfig<T> 扩展）
- [ ] 全局变量定义逐步迁移到 `fceu11::State` 内部

### 11.3 FCEUD_* 函数接口化

- [ ] 所有 `FCEUD_*` 全局函数替换为 `DriverCallbacks` 成员
- [ ] Qt 驱动注册自己的回调实现
- [ ] 核心不再 `#include` 任何驱动头文件

### 11.4 验收标准

- [ ] 核心源码零 `#ifdef __QT_DRIVER__` / `#ifdef __WIN_DRIVER__`
- [ ] `fceuWrapper.cpp` 拆分为 4 个独立文件，单文件 < 500 行
- [ ] 核心不 include 任何 `drivers/` 下的头文件
- [ ] `DriverCallbacks` 注册机制可工作
- [ ] 全部回归测试通过

---

## 12. v1.12 Scissors — 巨型文件拆分

**目标**：拆分超过 2000 行的巨型文件为职责单一的子模块。

### 12.1 TasEditorWindow.cpp (6,747行) 拆分

- [ ] 按 UI 面板拆分为子控制器：
  ```
  TasEditor/
    TasEditorWindow.h/cpp       — 主窗口框架 + 布局 (< 800 行)
    TasEditorPlayback.h/cpp     — 回放控制面板
    TasEditorTimeline.h/cpp     — 时间线面板
    TasEditorBookmarks.h/cpp    — 书签面板
    TasEditorBranches.h/cpp     — 分支面板
    TasEditorInputLog.h/cpp     — 输入日志面板
    TasEditorGreenzone.h/cpp    — Greenzone 管理
    TasEditorSelection.h/cpp    — 帧选择逻辑
    TasEditorUndo.h/cpp         — 撤销/重做系统
    TasEditorLua.h/cpp          — Lua 脚本集成
  ```
- [ ] 主窗口通过组合持有子控制器，信号/槽连接
- [ ] 共享状态通过 `TasEditorContext` 结构体传递

### 12.2 ConsoleWindow.cpp (3,999行) 拆分

- [ ] 按功能拆分：
  ```
  Qt/
    ConsoleWindow.h/cpp         — 主窗口框架 (< 600 行)
    ConsoleMenu.h/cpp           — 菜单栏构建与动作绑定
    ConsoleToolbar.h/cpp        — 工具栏
    ConsoleRecentRom.h/cpp      — 最近文件管理
    ConsoleEmulationControl.h/cpp — 模拟控制（暂停/帧步进/速度）
    ConsoleVideoConfig.h/cpp    — 视频配置面板
    ConsoleSoundConfig.h/cpp    — 声音配置面板
    ConsoleInputConfig.h/cpp    — 输入配置面板
  ```

### 12.3 其他大文件拆分

- [ ] `AviRecord.cpp` (~2800行) → `AviRecord.h/cpp` + `AviVideoCodec.h/cpp` + `AviAudioCodec.h/cpp`
- [ ] `ppu.cpp` (2191行) → `PpuCore.h/cpp` + `PpuRendering.h/cpp` + `PpuSavestate.h/cpp`（Ppu 类成员函数分布）
- [ ] `movie.cpp` (1759行) → `MovieCore.h/cpp` + `MoviePlayback.h/cpp` + `MovieRecord.h/cpp`

### 12.4 验收标准

- [ ] 无单文件超过 1500 行（目标均值 < 800 行）
- [ ] TasEditorWindow 拆分为 ≥ 8 个子控制器
- [ ] ConsoleWindow 拆分为 ≥ 5 个子模块
- [ ] 全部回归测试通过
- [ ] 编译时间不增加 > 10%（预编译头更新）

---

## 13. v1.13 Purify — 遗留 C 模式清理

**目标**：消除剩余的 C 风格模式，统一现代 C++/Rust 惯用法。

### 13.1 malloc/free 根除

- [ ] 全代码库审计，剩余 `malloc()/free()` 调用点清单
- [ ] 核心文件（src/ 根目录）：替换为 `std::make_unique` / `FceuMallocPtr`
- [ ] Qt 驱动文件：替换为 `std::vector` / `std::unique_ptr`
- [ ] Board 文件：v1.8 已完成
- [ ] 目标：零裸 `malloc()/free()` 调用（`FCEU_gmalloc` 包装器除外）

### 13.2 C 风格转换清理

- [ ] 全代码库审计 `(Type*)expr` 风格转换
- [ ] 替换为 `static_cast` / `reinterpret_cast` / `dynamic_cast`
- [ ] `/W4 /WX` 编译通过，无新 C-style cast 警告

### 13.3 #define → constexpr/inline 迁移

- [ ] 审计核心 `#define` 常量，替换为 `constexpr`：
  - `NTSC_CPU` / `PAL_CPU` → `constexpr uint32_t`
  - `FCEU_IQ*` IRQ 标志 → `enum class IRQSource : uint32_t`（已有 FCEU_ENUM_CLASS_BITFLAGS）
  - `JOY_*` 按钮位 → `enum class JoyButton : uint8_t`
- [ ] 宏函数替换为 `inline` 或 `constexpr` 函数
- [ ] 保留 `DECLFR`/`DECLFW` 宏（模拟器领域惯用法，替代成本 > 收益）

### 13.4 scoped_ptr.h 移除

- [ ] `scoped_ptr.h` → `std::unique_ptr` 全量替换
- [ ] `fceuScopedPtr` typedef → `using fceuScopedPtr = std::unique_ptr`
- [ ] 移除 `scoped_ptr.h` 文件

### 13.5 Lua 5.1 内嵌源码移除

- [ ] 删除 `src/lua/src/` 目录（32 .c, 23 .h）
- [ ] `FCEUX11_LUA_RUST_ENABLED` 成为唯一 Lua 路径（默认 ON）
- [ ] 移除 CMake 中 Lua C 源码的编译规则
- [ ] `src/lua-engine.cpp` 缩减或移除（功能已在 `fceux11-lua` Rust crate）

### 13.6 /wd 抑制项清理

- [ ] 逐项评估 `CMakeLists.txt` 中的 `/wd*` 选项：
  - `/wd4996` — 确认 strcpy/sprintf 已清除后移除
  - `/wd4100` — 未引用参数：函数签名加 `[[maybe_unused]]` 或移除参数名
  - `/wd4267` — size_t→int：全部修复后移除
  - `/wd4456` — 变量遮蔽：重命名后移除
  - `/wd4244` — 缩窄转换：显式 narrow_cast 后移除
- [ ] 目标：仅保留第三方代码（cbindgen, Qt moc）所需的抑制项

### 13.7 验收标准

- [ ] 零裸 `malloc()/free()` 调用
- [ ] 零 C 风格转换（`/W4` 下无 C-style cast 警告）
- [ ] 核心文件 `#define` 常量 ≤ 10 个（DECLFR/DECLFW 除外）
- [ ] `scoped_ptr.h` 已移除
- [ ] `src/lua/src/` 已移除，Rust Lua 为唯一路径
- [ ] `/wd` 抑制项减少 ≥ 50%
- [ ] 全部回归测试通过

---

## 14. v1.14 Anvil — 性能硬化与收官

**目标**：确保现代化改造后的性能不低于 v1.0，优化构建产出，确立 v2.0 准备状态。

### 14.1 性能基准全量复测

- [ ] 在与 v1.0 基线相同的硬件/配置下运行 `bench_cpu_frame`, `bench_ppu_frame`, `bench_full_frame`
- [ ] 对比 `baseline_v1.0.json`，任何退步 > 2% 必须修复
- [ ] 新增 APU 基准：`bench_apu_frame`
- [ ] 新增 Bus 基准：`bench_bus_dispatch`（ARead/BWrite 间接调用开销）

### 14.2 热路径优化

- [ ] 分析 `Cpu::run()` 内联展开情况，确保 MSVC 生成与 v1.0 等效代码
- [ ] `Bus::read()/write()` 确保 `__forceinline` 在热路径
- [ ] PPU 渲染循环确保无虚调用（`Ppu::loop()` 内部保持直接调用）
- [ ] 如有退步，使用 `__declspec(noinline)` 或 `#pragma optimize` 局部修正

### 14.3 LTO / PGO 优化

- [ ] 启用 MSVC Link-Time Code Generation (`/LTCG`) 在 Release 配置
- [ ] 建立 PGO (Profile-Guided Optimization) 训练场景：
  - 运行 Super Mario Bros. 3 (MMC3) 60 秒
  - 运行 Akumajou Densetsu (VRC6) 30 秒
  - 运行 Zelda (MMC1) 30 秒
- [ ] PGO 产出作为可选构建配置（`-DFCEUX11_PGO=ON`）

### 14.4 构建产物精简

- [ ] 审计链接库大小，移除未使用符号 (`/OPT:REF /OPT:ICF`)
- [ ] 确认 Rust staticlib 仅导出 FFI 所需符号
- [ ] 最终 exe 大小与 v1.0 对比，增量 < 15%

### 14.5 v2.0 准备状态检查

- [ ] `FCEUI_*` 别名全部标记 `[[deprecated("use fceu11::Foo")]]`（默认抑制，v2.0 启用）
- [ ] `FCEUX11_SHOW_DEPRECATION_WARNINGS=ON` 编译通过
- [ ] 全局变量兼容别名清单完整，可一次性移除
- [ ] savestate V2 格式为默认，V1 只读兼容

### 14.6 验收标准

- [ ] 所有性能基准与 v1.0 偏差 ≤ 2%
- [ ] LTO Release 构建可工作
- [ ] 最终 exe 功能完整，零回归
- [ ] `FCEUX11_SHOW_DEPRECATION_WARNINGS=ON` 编译零错误
- [ ] 全部回归测试通过
- [ ] v2.0 准备状态检查清单 100% 通过

---

## 附录 A：版本依赖图

```
v1.1 Sentinel ──────┐
                     ├── v1.2 Census ──────┐
                                         ├── v1.3 Legion (CPU) ──┐
                                                                 ├── v1.4 Gateway (Bus) ──┐
                                                                                         ├── v1.5 Prism (PPU) ──┐
                                                                                                                 ├── v1.6 Resonance (APU) ──┐
                                                                                                                                            ├── v1.7 Cartograph (Cart) ──┐
                                                                                                                                                                        ├── v1.8 Masonry (Board) ──┐
                                                                                                                                                                                                    ├── v1.9 Chronicle (Save) ──┐
                                                                                                                                                                                                                                ├── v1.10 Cryptex (ROM) ──┐
                                                                                                                                                                                                                                                          ├── v1.11 Bridge (Driver) ──┐
                                                                                                                                                                                                                                                                                      ├── v1.12 Scissors (Split) ──┐
                                                                                                                                                                                                                                                                                                                  ├── v1.13 Purify (Clean) ──┐
                                                                                                                                                                                                                                                                                                                                              ├── v1.14 Anvil (Harden)
```

> 严格线性依赖。每个版本的改动依赖前一版本奠定的基础。如资源允许，v1.5~v1.6 可与 v1.7~v1.8 并行开发（PPU/APU 与 Cart/Board 无直接依赖）。

## 附录 B：风险矩阵

| 版本 | 主要风险 | 缓解策略 |
|------|----------|----------|
| v1.3 | CPU 布局变更导致 savestate 不兼容 | `static_assert` 布局断言 + golden 往返测试 |
| v1.4 | Bus 间接层影响帧率 | `__forceinline` 热路径 + 基准监控 |
| v1.5 | PPU 像素级回归 | 逐帧 PNG 对比测试 |
| v1.6 | 音频采样级回归 | WAV 输出对比 |
| v1.8 | 171 个 board 文件批量修改 | 分批迁移 + 每 batch 独立回归 |
| v1.9 | Savestate 格式变更 | V1 加载 100% 兼容 + V2 往返测试 |
| v1.10 | ROM 解析迁移导致坏 ROM 误判 | Bad ROM 测试集 + 检测率对比 |
| v1.11 | 驱动解耦破坏 Qt 功能 | 手动 QA + 自动化 UI 测试 |
| v1.12 | 巨型文件拆分引入编译错误 | 预编译头更新 + 增量拆分 |

## 附录 C：C++ / Rust 职责划分（v1.x 收官态）

| 层 | 语言 | 职责 |
|----|------|------|
| 热路径 (CPU执行, PPU渲染, APU合成) | **C++** | 性能关键，保持过程式 + 直接内存操作 |
| 状态管理 (Cpu/Ppu/Apu/Bus/Cart 类) | **C++** | 封装全局状态，提供类型安全接口 |
| Bank-switching API | **C++** | Mapper 热路径调用，内联优化 |
| ROM 解析 (iNES/UNIF/FDS/NSF) | **Rust** | 安全解析、格式校验、bad ROM 检测 |
| Savestate 序列化 | **Rust** | 安全序列化、版本管理、压缩 |
| 工具 (CRC32/MD5/GUID/UTF/时间戳) | **Rust** | 已迁移完成 |
| 音频滤波 (FIR/SexyFilter/NeoFilter) | **Rust** | 已迁移完成 |
| 调试器 (汇编/反汇编/符号表/CD Logger) | **Rust** | 已迁移完成 |
| Lua 脚本引擎 | **Rust** (mlua) | 已迁移完成 |
| Qt GUI | **C++** (Qt6) | UI 框架绑定 |
| 视频缩放滤波 (hq2x/scale2x/NTSC) | **C** | 第三方算法，保持原样 |

## 附录 D：全局变量消除进度追踪

| 分类 | v1.0 数量 | v1.2 归档 | v1.3 迁入Cpu | v1.4 迁入Bus | v1.5 迁入Ppu | v1.6 迁入Apu | v1.7 迁入Cart | v1.14 目标 |
|------|-----------|-----------|--------------|--------------|--------------|--------------|---------------|------------|
| CPU | ~8 | ✅ | ✅ | — | — | — | — | 0 extern |
| Bus | ~20 | ✅ | — | ✅ | — | — | — | 0 extern |
| PPU | ~12 | ✅ | — | — | ✅ | — | — | 0 extern |
| APU | ~10 | ✅ | — | — | — | ✅ | — | 0 extern |
| Cart | ~15 | ✅ | — | — | — | — | ✅ | 0 extern |
| Config | ~6 | ✅ | — | — | — | — | — | 仅保留全局配置 |
| Debug | ~25 | ✅ | — | — | — | — | — | 仅调试构建可见 |
| **总计** | **~100** | **100%** | **8** | **20** | **12** | **10** | **15** | **≤ 6** |
