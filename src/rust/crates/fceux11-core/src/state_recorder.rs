//! StateRecorder (rewind / savestate history) — placeholder for v0.3.x migration.
//!
//! The C++ `StateRecorder` class in `src/state.cpp` remains in place for v0.2.29.
//! Rust-side state-file management (`state_file.rs`) already replaces the
//! compression and envelope layers that StateRecorder depends on.
